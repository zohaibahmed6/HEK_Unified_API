# HEK Unified Healthcare API — Project Status Log

This is the single source of truth for where this project stands. At the start of any new chat about this project, read this file first — Section 1 for the phase plan and current position, Section 2 for facts already established (don't re-derive them), Section 3 for open decisions blocking progress, Section 4 for the append-only history.

**Update this file at the end of every working session** (or when asked): move the current phase's status, add any new confirmed facts, add/resolve open decisions, and append a dated Change Log entry. Never rewrite past Change Log entries — only add new ones.

---

## 1. Phase Plan & Status

Lifecycle order per the Enterprise Technical Advisor skill. Do not skip a phase. Do not start implementation before Architecture (phases 4-9) is signed off.

| # | Phase | Status | Deliverable(s) | Location |
|---|---|---|---|---|
| 1 | API Analysis | ✅ Complete | 12 reports each for HISO, KARO, ERMS | `docs/analysis/{hiso,karo,erms}/` |
| 2 | API Comparison | ✅ Complete | Cross-system comparison | `docs/analysis/ComparisonReport.md` |
| 3 | Migration Classification | ✅ Complete | Keep/Refactor/Merge/Replace/Remove disposition per module | `docs/analysis/MigrationRecommendations.md` |
| 4 | Software Requirements Specification | ✅ Complete (v1.0 draft) | 22-section SRS, 47 "cannot be confirmed" flags | `docs/SRS_UnifiedHealthcareAPI.md` |
| 5 | **Enterprise Architecture** | ✅ Complete (2026-07-19) | Full 28-section Enterprise Architecture Document (EAD), Review Board scored **8 PASS / 4 WARNING / 0 FAIL** | `docs/architecture/Unified-Healthcare-API_EAD.md` |
| 6 | Database Architecture | ⏭️ **Skipped (stakeholder decision, 2026-07-19)** | No registry-DB design, no RLS-readiness verification performed. Legacy Indici schema/stored procedures connected to as-is (consistent with ADR-010). | — |
| 7 | API Contract Design | ✅ Complete, revised to v1.1 (2026-07-19) | Full API Contract Design Document (21 sections + Decisions 4–6) + companion OpenAPI 3.0.3 spec, now **42 endpoints**, verified in sync. v1.0's Contract Review score (4 PASS/3 WARNING/0 FAIL) has not been re-run against v1.1 — flagged as a small follow-up, not urgent. | `docs/architecture/Unified-Healthcare-API_API-Contract-Design.md`, `docs/architecture/Unified-Healthcare-API_openapi.yaml` |
| 8 | Security Architecture (detailed) | ⏭️ **Folded into Implementation Plan (stakeholder decision, 2026-07-19)** | No standalone threat-model doc; security tasks (OWASP checklist, secret mgmt, generic error handling, rate-limit toggle) embedded directly as Day-1 build-sprint tasks. | — |
| 9 | Logging & Observability Design | ⏭️ **Folded into Implementation Plan (stakeholder decision, 2026-07-19)** | No standalone doc; structured logging (Serilog), correlation IDs, health checks embedded as Day-1 Block 0 scaffolding tasks. | — |
| 10 | Infrastructure Design | ⏭️ **Folded into Implementation Plan (stakeholder decision, 2026-07-19)** | No standalone doc; Docker/docker-compose scaffold embedded as Day-1 Block 0 task, consistent with ADR-005/own-servers decision. | — |
| 11 | Implementation Planning | ✅ Complete (v1.0, 2026-07-19) | Day-1 build-sprint sequencing (4 blocks: scaffolding → security core → 18 domain endpoint groups in priority order → contract verification) + separate post-sprint hardening timeline (tests, security verification, load testing, staged rollout). | `docs/Unified-Healthcare-API_ImplementationPlan.md` |
| 12 | Development | 🟡 In Progress — Block 0, Block 1, Block 2 complete (2026-07-20) | New repo at solution root (`HekCoreApi.sln`); Block 0 (scaffolding), Block 1 (security core), and Block 2 (all 18 domain endpoint groups, 42 canonical operations) built and verified. Block 3 (contract re-verification) also complete — every built route cross-checked against the OpenAPI spec path-for-path/verb-for-verb; one real drift found and corrected in the spec itself (`/auth/token` 501, see openapi.yaml x-changelog 1.1.1), not left silently mismatched. Day-1 build sprint is now functionally complete end to end. | `HekCoreApi.sln`, `src/`, `tests/`, `docs/adr/ADR-012-*.md` |
| 13 | Testing | ⬜ Not Started | — | — |
| 14 | Performance Testing | ⬜ Not Started | Target: 10,000 concurrent users (no confirmed SLA/latency figures) | — |
| 15 | Security Review | ⬜ Not Started | — | — |
| 16 | Deployment | ⬜ Not Started | — | — |
| 17 | Production Readiness | ⬜ Not Started | — | — |

**Current position:** Phase 12 (Development) in progress. The full Day-1 build sprint — Block 0 (scaffolding), Block 1 (security core), Block 2 (all 18 domain endpoint groups), and Block 3 (contract re-verification) — is complete and verified (2026-07-20). Since then, a dedicated local database (`HekLegacyPmsDev`) was built from the inferred schema and a real Block 2 endpoint was verified end-to-end against real seeded data for the first time (2026-07-20) — see Change Log below for full detail, and open items 28/32 for what this does and doesn't prove. This is still Day-1 draft code, not production-ready: the post-sprint hardening stage (automated test suite, security verification, load testing, staged ADR-008 rollout) per the Implementation Plan §5 has not started.

**✅ Action item resolved (2026-07-19):** Both architecture documents are now in this project folder: `docs/architecture/Unified-Healthcare-API_EAD.md` and `docs/architecture/Unified-Healthcare-API_ADRs.md` (ADR-001 through ADR-011, confirmed present).

**⚠️ Accepted risk from skipping Phase 6:** The EAD's own §25/§27 flag an unverified assumption — that every table needing row-level security already carries a practice-identifying column — as something that should be confirmed "during the Database Architecture phase." That verification is now not happening as a dedicated step. Stakeholder decision: verify later if it becomes a problem, rather than up front.

---

## 2. Confirmed Facts (don't re-derive these)

### System overview
- Three legacy systems: **HISO** (WCF SOAP, ACC45 accident-claim forms, NZ HISO 10014.2 standard), **KARO/HSSWebAPI** (bridges Indici PMS to HSS portal), **ERMS/ERMSWebAPI** (eReferrals XML + undocumented COL/Pegasus JSON claiming integration).
- All three share a `DAL` codebase (identical subfolder/class names, identical hardcoded secrets, identical SQL-injection code smells in dormant modules).

### Production topology (⚠️ corrected 2026-07-19 — supersedes the SRS's stated assumption)
- The SRS assumed "one shared Indici PMS database" (§4.4). **This does not match real production**, per direct stakeholder confirmation: ERMS and KARO/HSS run from one app server that already routes to **different physical database servers depending on the calling practice**; HISO runs on **multiple IIS servers, each bound to its own specific database server**. Some database servers already host more than one practice's data together.
- The earlier finding that KARO and ERMS share identical code/stored-procedure names (`[HSS]` schema, `uspGetDemographics` etc.) is still accurate — that's a code-duplication fact, separate from physical topology, which is sharded.
- **Architecture decision (ADR-001):** No consolidation into one physical database. Instead: one small tenant/practice registry database (replacing `Web.config` connection-string lists) that the API looks up per request to route to the correct physical server; row-level security (RLS) applied on any server hosting more than one practice.

### Enterprise Architecture decisions (ADR log, approved 2026-07-19)
| ADR | Decision (one line) |
|---|---|
| ADR-001 | Tenant registry DB routes to existing sharded physical DB servers; RLS on shared servers. No data migration. |
| ADR-002 | Real auth via a managed identity platform (Entra ID/Okta/Auth0 — vendor TBD), fronted by a compatibility adapter so HSS Portal and ERMS keep their exact current request shape. |
| ADR-003 | Tokens scoped to one patient+encounter+practice; origin scope (KARO/HISO/ERMS) determined structurally by which credential/entry-point authenticated the request — never by a self-reported field. |
| ADR-004 | HISO keeps its existing session-GUID mechanism as-is; adds a 12-hour expiry and logs failed lookups as security events (currently silently swallowed). |
| ADR-005 | Docker containers, load-balanced across identical instances; cloud/host TBD. |
| ADR-006 | Balanced HA/DR: multi-region active-passive, pilot-light DR. Explicitly provisional — no real RTO/RPO figures exist yet. |
| ADR-008* | Every new capability, including auth/security itself, ships behind a config toggle (off by default) — flagged by the Review Board as the one item needing an owner and enable-by date before production. |

*ADR numbering in the source document runs to ADR-011 (routing, HTTPS-everywhere, no schema changes, ERMS/HSS failover pair) — full detail is in the ADR document once it's copied into this folder (see Action Item above).

### ADR follow-up round decisions (`Unified-Healthcare-API_ADRs.md` §7/§9, approved 2026-07-19 — corrects §3 rows that were left stale after this round)
| Item | Resolution |
|---|---|
| Identity-service vendor | **Microsoft Entra ID** |
| Human logins vs. legacy service accounts (hsslive, ERMS/COL) | **One identity service, different account types** — not split into two systems |
| COL/Pegasus origin-scope | **Separate from ERMS's other eReferrals functions** (financial `SaveInvoice` write) |
| Token early-revocation ("kill switch") | **Not included** — tokens simply expire naturally |
| `SessionGUID` randomness | **Confirmed strong** — not a weak generator |
| Compliance/data-residency regime | **NZ Health Information Privacy Code applies** |
| Uptime/downtime tolerance | **24/7, zero downtime** (superseded ADR-006 to active-active) |
| Dormant DAL modules (BPAC, BPI, GP2GP, MHNAppointment, MHNHL7, Procare, Procon, Screening, UI, DMSAWS) | **Retained** — confirmed needed for future use, not removed |
| HISO database routing (no practice identifier in its calls) | **Resolved — ADR-007**, route by existing per-server address, fallback search on mismatch |
| Rollout approach for all new capabilities, incl. security | **Config-driven, defaulted off, including security — ADR-008** (risk explicitly accepted) |

### Security findings (hard blockers — from Phase 1-3 analysis, unchanged)
- Identical hardcoded SQL symmetric-key password (`tcpepms*1`) shared across HISO/KARO/(likely)ERMS.
- KARO and ERMS ship byte-identical DAL code including a live SQL injection in `DMSDA.cs` (confirmed via direct inspection).
- No real authentication in any of the three systems today — addressed by ADR-002/003/004.
- Thread-unsafe static shared DB connection in HISO (`DbAccess.cs`).
- ERMS has the most exposed credential footprint: 15 connection strings in `Web.config`, several pointing at a public IP.
- All three: no automated tests, no rate limiting, no security-event/audit logging, PHI/secrets logged in plaintext.

### API Contract Design decisions (Phase 7, approved 2026-07-19)
| Decision | Resolution |
|---|---|
| Response format (SRS §13.3, previously deferred) | One canonical JSON/REST contract internally; every existing consumer (HSS Portal, ERMS eReferrals, COL/Pegasus, HealthLink engines) keeps its exact current wire shape via edge compatibility adapters — resolved by inference from ADR-002/004/007's established "zero consumer-side change" pattern, not asked as a fresh question. |
| Versioning | **Stakeholder decision:** no version identifier in URL/header/media-type; version tracked in documentation only (OpenAPI `info.version` + changelog). No request/response change may ship without a coordinated migration. |
| Error format | **Stakeholder decision:** errors must not display any data. RFC 7807-inspired shape, but `detail`/`errors[].message` are always generic/static — never PHI, never exception text. Full detail goes to server-side structured logs only, correlated via `traceId`. |
| Pagination | **Stakeholder decision:** none in v1 — third-party consumers have no pagination handling on their end. List endpoints return the full filtered set inside `{"items": [...]}`, left pagination-ready for a future non-breaking addition. Accepted as a scalability risk, not an oversight (see Contract Design doc §19). |
| Idempotency mechanism | Optional `Idempotency-Key` header + natural-key fallback (for legacy edge adapters that won't send the header); duplicate write returns `200` with the existing resource, replacing KARO's `-5`/`-3` and ERMS COL's `-3` magic codes. |
| Document classification (FR-DOC-03) | Both `direction` (in/out) and `contentType` (MIME) kept as independent first-class fields on the canonical `Document` schema — shape resolved, but the combined filing/mapping *logic* is left open for Implementation Planning (no source document specifies it). |

### Stakeholder decisions on genuinely-open items (2026-07-19, asked directly per "what's left, ask me")
| Item | Decision |
|---|---|
| **General principle (stated explicitly 2026-07-19, applies platform-wide, not just to one endpoint)** | **Do not remove anything from the new API that any old API already implemented.** Applies beyond KARO's `SaveInvoice` field list — this is a standing constraint on every contract/schema decision going forward: legacy fields/capabilities get carried forward or explicitly reconciled, never silently dropped. |
| ADR-008 security-toggle ownership | **Zohaib owns it and will flip it himself** when ready — no fixed calendar date is being tracked; this is a self-managed action, not a delegated one. Treated as closed rather than "date TBD." |
| **Code preservation (broadened, 2026-07-19)** | **Do not remove any code from the previous APIs — it has impact.** Broadens the earlier "don't remove implemented capabilities/fields" principle to the code level: nothing gets deleted outright, even code that looks dead or unreachable. ✅ **Resolved 2026-07-19:** Zohaib confirmed explicitly — "no injections don't stay as is." The dormant DAL modules (`DMSDA.cs`, `DBMessages.cs`) are **kept in the codebase, nothing deleted**, but the confirmed SQL injection inside them **must be fixed (parameterized) before being carried forward** — satisfying both "don't remove old code" and the pre-existing Phase A hard-blocker (SRS §5.4/§12.6, Migration Phase A item 4). Not a blanket "reproduce everything exactly," specifically for confirmed security vulnerabilities. |
| Azure ERMS mirror liveness | **Not live** in production today, but **do not remove the capability** from the unified platform — keep the forwarding mechanism/code path present (dormant-safe, not deleted). |
| COL/Pegasus consumer liveness | **Live.** Confirmed active — the 7 `COLController` endpoints (including the financial `SaveInvoice` write) stay in scope as real, active capability, not a dormant/legacy-only concern. |
| Cloud provider / container host | **Own servers for now** (on-prem/self-hosted), built to stay cloud-agnostic so a future move to AWS, Azure, or Google Cloud doesn't require a redesign — Docker (ADR-005) already supports this; no cloud-vendor-specific service should be depended on without a portable alternative. |
| Aspose licensing/rendering tool | **Keep, implemented as the old system did it.** No replacement — HISO's existing Aspose-based HTML/image→PDF/DOCX rendering approach carries forward as-is (still subject to the licensing/version refresh already flagged as a known gap in the SRS, ADR-010's "no schema change" constraint doesn't apply here since this is a rendering library, not a DB decision). |
| HISO dead code (`static mode`, `addInvoice`, `launchForm`, unreachable `saveProcessAction` code) | **Implement it — do not skip anything the old system implemented.** This reverses the API Contract Design doc's Section 4.9 exclusion (previously "excluded — confirmed dead no-op stubs, pending sign-off"). The contract document needs a follow-up revision to bring these back into scope as real, working endpoints/actions. |
| KARO `GetEncounterSummary` / `SaveScreeningCode` | **Implement — do not skip.** Reverses the Contract Design doc's Section 4.10/4.13 exclusion of both as "confirmed broken, not ported." Since the legacy behavior itself is confirmed non-functional/insecure (`GetEncounterSummary` returns hardcoded mock data; `SaveScreeningCode` skips auth and doesn't persist — FR-AUTH-04 explicitly bars reproducing that), this is understood as "build the real, working capability neither legacy system actually delivered," not "port the broken behavior verbatim" — **flagged for stakeholder confirmation of that reading**, since the literal instruction ("implement as it is") could also be misread as reproducing the broken behavior, which would conflict with FR-AUTH-04. |

### Demographics (and other union-shaped) response reconciliation — deferred, not merged (2026-07-19)
- **Zohaib decided: keep HISO's, KARO's, and ERMS's response shapes separate for now** rather than have the contract force them into one merged/canonical field set, because he doesn't currently have sample live responses or the time to do the field-by-field comparison. To be revisited later.
- **This is a real change to the API Contract Design document**, not just a closed checklist item. The document as currently written (Section 6.2, `GET /patients/{patientId}/demographics`) assumes one canonical merged shape produced by edge adapters translating *into* it. "Keep separate" instead implies either: (a) the canonical endpoint returns whichever legacy system's shape matches the caller's origin scope, unmerged, or (b) three distinct endpoints/response variants exist side by side until reconciliation happens. **Resolved 2026-07-19: Option B — "as previously implemented."** Each legacy system keeps its own separate endpoint/shape (HISO's `getData`-based demographics, KARO's `GetDemographics`, ERMS's `GetPatientData`) rather than one shared canonical path that switches shape by caller origin. No single merged `/patients/{patientId}/demographics` endpoint — this needs to be undone in the contract document, which currently only has the one merged version. Joins the still-pending contract-document revision action item (alongside the earlier KARO/HISO dead-code un-exclusions).

### SRS key facts
- Target: **10,000 concurrent users**, 24/7, highly available, horizontally scalable — no specific latency/throughput/SLA figures documented (SRS §5.5).
- Migration phases: **Phase A (hard blockers)** = real auth, eliminate unsafe shared connection, eliminate hardcoded secrets, fix SQL injection, correct HTTP status codes, narrow CORS, rate limiting. **Phase B** = canonical read-surface merge, COL/Pegasus consolidation, doc-gen consolidation, structured logging, dormant-DAL retirement decision, tenant onboarding model, test suite.

---

## 3. Open Decisions (blocking or upcoming)

| # | Decision | Status | Notes |
|---|---|---|---|
| 16 | Field-by-field reconciliation of demographics (and other union-shaped) responses across HISO/KARO/ERMS | ✅ Deferred, not reconciled (2026-07-19) | **Zohaib decided: keep the three APIs' response shapes separate for now** — no merged/canonical demographics field set — since he doesn't currently have sample responses or the time to do the comparison. Explicitly a "check this in future" deferral, not a decision to merge blindly. **This changes the contract's approach**, not just this one open item — see §2 and the Contract Design doc follow-up note below. |
| 17 | ERMS/COL's `SaveInvoice` request field list and real backing DAL call (originally logged as "KARO's SaveInvoice" - corrected once the real source was found, this is ERMS's COL/Pegasus integration, not KARO) | ✅ **Resolved (2026-07-20)** | Zohaib supplied the real source: `legacy-reference/controller/COLController.cs` (`SaveInvoice()` action) and `legacy-reference/DAL/HSS/HSSDA.cs` (`InsertUpdateService`). Confirmed the real stored procedure is `[OnlineClaim].[uspInsertUpdateService]`, not the `[HSS].[uspSaveInvoice]` name previously guessed. Confirmed three fields the old inferred contract had silently omitted (`Description`, `AccountHolderId`, `ServiceProviderType`) - added to `InvoiceInput`/`Invoice` and the OpenAPI spec (bumped to 1.1.2) per the "don't drop what the old system implemented" rule. Also confirmed two legacy quirks preserved as-is in `InvoicesRepository.SaveAsync`: `masterServiceName` is hardcoded to the literal `"COL"` by the legacy controller (never caller-supplied), and `locationId` is always empty/`DBNull` (also never caller-supplied) - both carried forward unchanged since that's what the real code does. **KARO confirmed (Zohaib, 2026-07-20) to use the same underlying method as ERMS/COL** - consistent with the already-documented fact (§2) that KARO and ERMS share one DAL codebase (`[HSS]`-schema procedures, identical hardcoded secrets, identical dormant-module code). No separate KARO-specific SaveInvoice source was supplied or exists to find - `InvoicesRepository.SaveAsync`'s field list/procedure mapping applies to both calling paths, not just ERMS/COL. Item fully closed, no remaining KARO-specific gap. |
| 18 | Specific rate-limit thresholds | ✅ **Resolved (2026-07-20)** | No source document has quantitative traffic figures (SRS §5.5/§19: "cannot be confirmed from analysis" beyond a qualitative 10,000-concurrent-users target with no request-rate breakdown) — fabricating a "final" number with no evidence behind it would violate the project's standing rule against inventing unconfirmed business values. Asked Zohaib directly rather than guessing. **Zohaib confirmed: formalize the current generous defaults (10,000 requests/60s per IP) as the deliberate Day-1 setting**, not an unconfirmed placeholder — this matches the Contract Design doc's own already-approved rollout decision (Section 14: "generous/effectively-off at launch, tightened once monitoring confirms real thresholds are safe"). `RateLimitOptions.cs` doc comment updated to reflect this is now a confirmed decision, not an open gap. Toggle (`RateLimit:Enabled`) remains off by default. **Revisit once real production traffic/monitoring data exists** to tighten safely - that data doesn't exist yet, so there's nothing to tighten to right now. |
| 20 | ERMS's hardcoded PHO-override parity with KARO (practice `302_F3H045`) | ✅ Closed (2026-07-19) | **Zohaib: if the override lives in `Web.config`, ignore it** — not worth verifying. No longer tracked as an open item unless it turns out to be code-level, not config. |
| 21 | `GetEncounterSummary`/`SaveScreeningCode` and rate limiting | ✅ Resolved (2026-07-19) | Zohaib adopted the recommendation: **implement both endpoints for real** (working auth check, working persistence/data — not the old mock/no-op behavior), keeping the existing endpoint names/paths/request shapes for continuity. **Rate limiting gets built**, applied via the same config-toggle pattern as auth/origin-scoping (ADR-008) — generous/off-by-default at launch, tightened once proven safe, rather than permanently absent. Nothing changes for existing callers on day one either way. |
| 23 | Dormant DAL modules (`DMSDA.cs`, `DBMessages.cs`, + 8-10 others) — Block 1's SQL-injection-fix deliverable | 🟡 **`DMSDA.cs` closed; `DBMessages.cs` deferred to Block 2 (Zohaib's decision, 2026-07-20)** | Zohaib supplied the real legacy source for both files. **`DMSDA.cs` ported** to `src/Infrastructure/Legacy/Dormant/Dmsda/DmsDocumentService.cs` — the confirmed injection (`UpdateInboxFolderDocuments`'s string-concatenated `CommandText`, itself dead/unreachable code in the original) is fixed via full parameterization; a unit test (`Infrastructure.UnitTests/Legacy/DmsDocumentServiceTests.cs`) proves a SQL-metacharacter payload is treated as an ordinary value, not syntax. Registered in DI, not wired to any endpoint (still dormant). This closes the `DMSDA.cs` half of SRS Phase A hard-blocker item 4. **`DBMessages.cs` explicitly deferred to Block 2**: as supplied it contains no string-concatenated SQL (already parameterized via an unsupplied `DALHelper` class), so the "confirmed injection" framing doesn't match this specific file; it's also HISO's live concept-mapping engine, coupled to types not present anywhere in this repo (`HealthLinkSession`, `HisoRequest`, `DynamicParam`, `AWSDoc.IndiciDMS`, `Hiso.ConceptMapper`). Rather than invent those types' shapes now, **Zohaib decided: leave it for Block 2**, where it naturally belongs alongside the real ACC45/HISO domain endpoints and their real supporting types. See ADR-012 Decision 8 (updated) for full detail. |
| 24 | Tenant registry DB exact schema (`PracticeRegistryEntry`) | ✅ **Signed off as new design (2026-07-20)** | Re-checked the EAD and full ADR-001 text directly before closing this - confirmed no source document anywhere gives a literal column list, only ADR-001's prose ("one row per practice with which physical database server it lives on"). Since no real schema exists to confirm against, closing per item 24's own stated resolution path ("confirm... or explicitly sign off as new design"): Block 1's schema (`PracticeId, PracticeName, SourceSystem, DbServerHost, DbName, RowLevelSecurityEnabled, IsActive, CreatedAtUtc, UpdatedAtUtc`) stands as the deliberate design. See item 31 below - the schema itself was fine, but the admin API surface built on top of it had a real bug, now fixed. |
| 25 | HISO server-address-to-database routing map, and the `SessionCreatedAtUtc` column used for the new 12-hour expiry | 🟡 Built on an inferred design | ADR-007 states routing is "by which address was called" but gives no concrete address list; ADR-004's new expiry needs a session-creation timestamp not confirmed present on `tblHealthLinkSession` (only `ProviderID/PatientID/AppointmentID/PracticeID` are confirmed). Both shipped as configurable placeholders needing verification against the live HISO deployment/schema. See ADR-012 Decision 7. |
| 26 | Canonical `POST /auth/token`'s origin scope for a direct (non-legacy) caller | 🔴 Unresolved | The OpenAPI `TokenResponse.originScope` enum only defines `Hiso\|Karo\|Erms\|Col` — no source document says what a direct caller of the canonical endpoint (not through a legacy compat endpoint) should get. Endpoint returns `501 Not Implemented` pending this decision rather than inventing a fifth value. See ADR-012 Decision 4. |
| 27 | Legacy compat auth endpoints (`/karo/authenticate`, `/erms/authenticate`, `/erms/col/authenticate`): path namespacing and always-200 status-code behavior | 🟡 Built on a reasoned default, flagged for confirmation | Legacy paths were literally bare `/authenticate` on each system's own dedicated server; this unified single-host deployment namespaces them by system prefix to avoid collision, and preserves each system's exact legacy response body/status-code behavior (including always-200) under the "zero consumer-side change" principle rather than applying FR-HTTP-01's new status-code semantics to these specific compat endpoints. ADR-011's designated-server/host-based routing may be the intended real mechanism instead of a path prefix. See ADR-012 Decisions 5-6. |
| 28 | Every Block 2 domain repository's stored-procedure and result-column names | 🟡 **First endpoint confirmed against real production data (2026-07-20)** — most repositories still unverified | No live DB/schema access is available anywhere in this project (SRS: "the actual T-SQL definitions... were not available"). All ~35 procedure names across the 18 domain groups follow one of two confirmed conventions (`[HSS].[uspX]` for KARO/ERMS, confirmed via `uspGetDemographics` in PROJECT_STATUS.md §2; `Hiso.uspGetPatient_X` for HISO, confirmed via DBMessages.cs's AWS-enabled-procedure list). **2026-07-20 update 1:** the full inferred schema was built end-to-end on a new dedicated local database (`HekLegacyPmsDev`) and a real Block 2 endpoint was verified to return real seeded data through the complete chain - proved the *code* is internally consistent, not that it matches the real schema. **2026-07-20 update 2, a real step forward:** Zohaib repointed the test practice at the actual `PMS_NZ_V2` database and supplied a real patient ID (2459731). Tested KARO/HISO/ERMS demographics directly against it: `[HSS].[uspGetDemographics]` (KARO) **is a real, executing procedure returning real data** - but its actual result columns are `Given`/`Family`/`BirthDate`, not the assumed `FirstName`/`LastName`/`DateOfBirth` (those column names exist too, but hold an unrelated composite internal-reference string, not a usable value - confirmed by dumping every real column name/value returned, not guessed). Fixed in `DemographicsRepository.GetKaroAsync`; `GET /patients/2459731/demographics/karo` now returns `200` with real production data. `Hiso.uspGetPatient_Demographics` and `[HSS].[uspGetPatientData]` (ERMS) were both tested too and confirmed **not to exist** under those names in `PMS_NZ_V2` (SQL error 2812) - genuinely wrong guesses, now known rather than assumed. This is the first Block 2 repository fully verified against live production data; the other ~34 procedures across 17 domain groups remain unverified. Still the single biggest hardening-phase dependency, but no longer purely theoretical for at least this one case. |
| 29 | `billing:write` scope is never granted by the token issuer | ✅ **Resolved — requirement removed (2026-07-20)** | Zohaib pushed back directly: "implement as it is, as legacy system was working... I do not understand why you are asking for billing rights." Checked the actual basis for the requirement before changing anything: SRS §12.2 recommended a distinct billing scope citing `erms/SecurityAnalysis.md` SEC-04 as evidence — but SEC-04 is actually about wildcard CORS on `COLController`, not a missing-authorization finding, and the real legacy `COLController.SaveInvoice` (confirmed from source) required only the same session-token validation every other endpoint used, no distinct permission tier at all. So this was a new requirement the earlier SRS phase recommended, not something the legacy system had. Zohaib decided to match the legacy system's actual behavior instead of the SRS recommendation: removed `[Authorize(Policy = BillingWrite)]` from `InvoicesController`, which now falls back to the same `ResourceScoped` policy every other Block 2 write endpoint uses. The `BillingWrite` policy constant/registration is left in place, unused, in case a real scope-granting design is wanted later. OpenAPI spec updated to match (1.1.3). |
| 30 | HISO's ACC45 `saveContainer`/form-save Aspose rendering step | 🔴 Not implemented, by design — **reconfirmed (2026-07-20)** | Aspose is commercially licensed; no license/package is available in this environment. Zohaib reconfirmed directly: "Keep the placeholder... Do not substitute Aspose with another PDF library... We'll integrate the real Aspose rendering once a valid company license is available." `Acc45Repository.SaveFormAsync` implements the real HISO-BR-12 DMS-then-persist sequencing but stores the form's raw JSON instead of a rendered document - the rendering call is now bracketed with an explicit `// TODO(Aspose)` marker isolating exactly what to swap later (bytes/extension/description only) without touching the surrounding save/DMS sequencing. Needs Aspose licensing before this is production-capable. |
| 31 | `/admin/practices` `PracticeInput.databaseServerId` vs. Block 1's `PracticeRegistryEntry` schema (`DbServerHost`/`DbName`/`SourceSystem`) | ✅ **Resolved for real, not just reconciled on paper (2026-07-20)** | This turned out to be an actual correctness bug, not just a documentation mismatch: the old mapping set `DbName = DatabaseServerId` (the same value as `DbServerHost`), so `ILegacyPracticeConnectionResolver`'s `Server={DbServerHost};Database={DbName};...` would have produced a broken connection string for any real practice registered through this endpoint (a database name is never the same string as its server's hostname) - this was undiscovered until now because `/admin/practices` had never actually been exercised end-to-end. **Fix**: `PracticeInput`/`Practice` (`src/Contracts/Admin/`) now carry `DbServerHost`, `DbName`, and `SourceSystem` (typed as the existing `OriginScope` enum - `Hiso\|Karo\|Erms\|Col` - reusing a type that already existed rather than inventing a new one) as three explicit fields instead of one collapsed `databaseServerId`. `PracticeAdminRepository` maps them 1:1, no more duplication or `"Unknown"` default. OpenAPI spec updated to match (bumped to 1.1.4, x-changelog entry). Validators updated. Solution rebuilt clean, full test suite green (11/11 across all test projects). |
| 32 | Legacy compat auth translators (`HssAuthenticateTranslator` and siblings) never map a `practiceId` into the issued token | 🟡 **Scaffolded as a config toggle, OFF by default (Zohaib's decision, 2026-07-20)** | Re-read the actual source docs precisely: neither KARO's nor ERMS's Authenticate *request* payload ever includes a practiceId (confirmed - only username/password/patientId/encounterId/system/pho for KARO, username/password/patientId/encounterId for ERMS), but both systems' *responses* include a server-resolved `practiceId` (`KARO_HSS_doc.md`: `"practiceId": "demo"`; `ERMS_doc.md`: `<PracticeId>demo</PracticeId>`) - the old backend looked it up internally from something (probably `pho` or `username`), and that lookup's source was never supplied. Zohaib: "leave it configurable and turn off, we will check this later, it should work as it was working in legacy." Built `LegacyPracticeResolutionOptions` (`Auth:LegacyPracticeResolution:Enabled`, default `false`, ADR-008-style toggle) - while disabled, behavior is unchanged (empty practiceId, same as before this existed). When enabled, `AuthenticateCommandHandler` looks up `Auth:LegacyPracticeMappings:{username}` via `ISecretProvider` and uses it if found - keyed by username since that's the one field confirmed present on every legacy Authenticate payload, **not because username has been confirmed as the real lookup key** (could plausibly be `pho` instead - Zohaib gave one example pair, practiceId `901` / key `FZZ999-B`, whose format doesn't match KARO's documented 4-letter `pho` codes like `NBPH`, so the exact key type is still unconfirmed). Two new unit tests prove both toggle states behave correctly. Real mapping data and confirmation of the lookup key still needed before enabling. |
| 33 | `docker compose up` (api service) cannot open any outbound TCP connection from inside the container, on this dev machine | 🔴 Unresolved, newly discovered (2026-07-20) | Docker Desktop was turned on and `docker compose up` was run for the first time. `sqlserver` builds/starts/healthchecks cleanly and is independently confirmed reachable (via `sqlcmd` from within its own container over the `sqlserver` network alias, and via `nc` from an unrelated third-party `busybox` container on the same bridge network — both succeed instantly). But the `api` container's own .NET process fails **every** connection attempt to `sqlserver` with `SqlException ... TCP Provider, error 40`, and `ClientConnectionId` is always all-zeros (TCP never actually established). Ruled out as the cause, each individually retested: DNS/hostname resolution (works — `getent hosts sqlserver` resolves correctly), TLS/encryption (`Encrypt=False` added, no change), IPv4-vs-IPv6 socket preference (`IPAddressPreference=IPv4First` added, no change), connecting by raw container IP instead of hostname (no change), connecting via `host.docker.internal` instead of the internal bridge network entirely (no change). `apt-get` inside the same `api` container to install `nc` for a same-image control test also failed with the same broad "connection refused" pattern for real internet egress, while intra-bridge traffic to `sqlserver` works fine for *other* containers - suggesting something specific to this container/image (or a host-level Docker Desktop network-isolation feature, e.g. Enhanced Container Isolation, a corporate VPN/EDR agent, or a WSL2 vEthernet quirk) is blocking outbound TCP specifically from the built `api` image, not a code defect. Reverted the exploratory `Encrypt=False` connection-string change (didn't fix anything, not worth keeping). Stack torn down (`docker compose down`) after diagnosis rather than left running broken. **Needs Zohaib to investigate on the host side** (Docker Desktop's Enhanced Container Isolation setting, antivirus/EDR/VPN container network filtering) since this is specific to the local machine's Docker networking stack, not something further code changes are likely to fix. `docker compose up` end-to-end verification remains genuinely unverified. |
| 34 | Data-retrieval operations (not just auth) never got legacy wire-compatible treatment | 🟡 **HISO's `getData` fully rebuilt against real legacy source and verified (2026-07-20); getVersion/getDeliveryOptions/saveContainer/getFormView/processAction, plus KARO/ERMS, still open** | Original scope (Zohaib flagged the canonical REST endpoints use different names/paths/protocols than legacy) expanded once Zohaib supplied **the entire real legacy HISO Visual Studio project** (`legacy-reference/Hiso/` - `FormSessionService.svc.cs`, the real `ConceptMapper`, all Acc45/Patient builders, `Task.cs`, `DocumentHandler.cs`, `Mapper.cs`, real `Web.config`) and gave a direct standing instruction: match legacy exactly, including its quirks/bugs, for every fork - documented in full in the approved plan at the time (`swift-painting-blossom.md`). **`getData` is now a genuine, real port**, not a name-guessing approximation: real `[Hiso].[UspGetHisoConcepts]` concept dictionary (10-min cache, matching legacy exactly - replaces the earlier `Hiso.uspGetPatient_{ConceptName}` guess), real qualifier-ID/conceptName/conceptID priority matching (`HisoRequestEngine`, ported from `HisoConceptDetail.cs`'s `HisoRequest` class), real group-concept XML cloning, real `IsDynamic=="1" AND operationMode=="N"` gating (every other combination is a genuine empty stub in legacy too - correctly reproduced as such, not built as new functionality), and real MIME-type routing (byte-level PDF conversion is Aspose-blocked, same as ACC45 item 30 - routing/dispatch is real, the render step is a flagged placeholder). Verified end-to-end against real seeded data (`db-scripts/05_HisoConceptDictionary.sql`, new) with a real multi-field XML request - `FirstName`/`LastName`/`DateOfBirth` all resolved and filled correctly through the real engine. **Also discovered**: `HisoSessionResolver`/`ResolveHisoSessionQuery` (Block 1) had never been wired to any HTTP endpoint before this work - genuinely dormant, not just unconfirmed. **Resolved via direct decision, not guessed**: `processAction`'s `addInvoice`/`launchForm` revert to legacy's real empty no-op behavior in this compat endpoint specifically (confirmed genuinely unimplemented in real source), even though the existing REST `/acc45/...` endpoint keeps a real implementation - Zohaib chose legacy fidelity as the tiebreaker. **2026-07-20 update 2**: `getVersion` and `getDeliveryOptions` built and verified for real - both are config/hardcoded logic, no builders needed (`getVersion` returns legacy's exact hardcoded values; `getDeliveryOptions` sources from `ISecretProvider`, real conditional branching on session `PracticeEDI` reproduced exactly, including a schema extension - `EDIAccount` column added to `tblHealthLinkSession`, confirmed real from `Mapper.cs`'s `HealthLinkSession.GetByGUID`). `processAction`'s `addTask` branch built and verified for real too (`Task.cs` port: real SNOMED/Read-code lookup via `Billing.usptblConcept_GetByCode`, real `Task.uspAddTaskExternal` save - confirmed with a real DB row, subject correctly concatenated as `"{SNOMED name}<br/>{description}"` matching legacy exactly). `addInvoice`/`launchForm` confirmed returning `processed=false` with no persistence, matching legacy's real no-op behavior per the earlier decision. `processAction`'s `"save"` branch (needs the 5-builder patient/consult/employer/problem/practitioner pipeline) still throws `NotImplementedException` when `IsDynamic` is on rather than silently claiming success - flagged, not faked. **Still open**: `saveContainer`, `getFormView`, and `processAction`'s `"save"` branch need the same real-source-grounded rebuild (8 builder classes, `DocumentHandler.cs`, remaining `Mapper.cs` helpers - full detail in the approved plan file); AWS document flow (Zohaib has `AWSDoc.IndiciDMS` as a compiled DLL, will map it in later - calling-code orchestration hook built, no implementation yet); second database node (Zohaib will supply the real connection string later - routing hook built, unconfigured); KARO's/ERMS's own data-retrieval operations have the identical gap and haven't been started. |

**Reconciled this session (moved to §2 Confirmed Facts, no longer open):** identity-service vendor, human-vs-service-account tenancy, COL/Pegasus origin-scope, token revocation, `SessionGUID` randomness, compliance regime, uptime/downtime target, dormant-DAL-module disposition, HISO routing model, rollout/toggle approach, ADR+EAD document location, security-toggle ownership, Azure-mirror liveness (not live, keep capability), COL/Pegasus liveness (live), Aspose (keep, as old), HISO dead-code disposition (implement, don't skip), and the general "don't remove anything old APIs implemented" principle. These were resolved in the ADR follow-up round or this session's direct Q&A but had been left stale, or are newly closed as of 2026-07-19.

**As of 2026-07-19, all items in this table were closed** (demographics kept separate as previously implemented; PHO-override check dropped if Web.config-only; KARO's `SaveInvoice` fields kept as-is once found; `GetEncounterSummary`/`SaveScreeningCode` built for real; rate limiting built, config-toggled like auth; cloud host, Aspose, security-toggle ownership, Azure/COL liveness, and the SQL-injection-vs-code-preservation question all closed). Block 2 (2026-07-20) then opened items 28-31 as flagged inferences rather than blockers, and the 2026-07-20 database-wiring work opened item 32 (compat-auth practiceId gap). Items 26, 27, 28, 30, 33, 34 remain genuinely open as of 2026-07-20 (items 17, 18, 24, and 29 closed, item
31 fixed as a real bug, item 32 scaffolded-but-disabled this session — see Change Log) — see rows
above.

---

## 4. Change Log

*(Append-only. Add a new entry at the top for each session that changes project state. Never edit past entries.)*

### 2026-07-21 (HISO wire-compat rebuild completed for real logic + verified against the real production-grade legacy DB, item 34)

- **Ported the remaining 8 real builder classes** completing item 34's full scope:
  - `saveContainer`: `Acc45DefinitionRepository` (`Appointment.usptblACC45DefinitionInsert`/`usptblACC45Definition_Get`), `Acc45DetailRepository` (Acc45Builder/Acc45DiagnosisBuilder/Acc45ReferralBuilder ported into one repository, real `Appointment.usptblACC45Detail_InsertUpdate_New`, all 3 confirmed business rules applied - `employmentStatus` splitting, diagnosis `side` splitting, referral `treatmentProvider` first-portion rule). Response is hardcoded `true` regardless of actual save outcome, matching a real legacy bug on purpose.
  - `processAction`'s `"save"` branch: `HisoProcessActionSaveRepository`, all 5 real builders (`PatientBuilder`→`Hiso.uspPatient_Update`, `PatientConsultBuilder`→`Hiso.uspPatientConsult_Update`, `PatientEmployerOrganisationBuilder`→`Hiso.uspPatientEmployerOrganisation_AddUpdate` with 3 Country/City/Suburb/Designation lookups against a separate Indici Master connection, `PatientProblemBuilder`→`Hiso.uspPatientProblem_Add`, `RegisteredPractitionerBuilder`→`Hiso.uspRegisteredPractitioner_Update`). Individual builder failures propagate except `PatientEmployerOrganisationBuilder`, which swallows its own exceptions exactly like legacy. The outer `"save"` action always returns `true` once all 5 have run, matching legacy's own discarded-result quirk.
  - `getFormView`/`getVersion`/`getDeliveryOptions` endpoints completed earlier this block; all 6 real `FormSessionService` operations are now implemented against real ported logic, no operation left throwing `NotImplementedException`.
- **Zohaib supplied real legacy connection strings** (`ConectionStringPMS_NZ`→`dbserver-local/PMS_NZ_V2`, `ConectionStringPMS_NZ_SecondNode`, `ConectionStringIndici_Master`→`192.168.0.6\sql2014/Indici_Master`, `ConectionStringPMS_NZ_DMS`→`dbserver-local/DMS_PMS`) and asked to connect to the real DB rather than fabricate schema, "to avoid issues and reduce token usage."
- **Found and fixed a real pre-existing bug while wiring this up**: `HisoSessionRepository` (session-GUID resolution used by every HISO compat operation) did a raw `SELECT ... CreatedAtUtc, EDIAccount FROM tblHealthLinkSession` - neither column exists on the real table (confirmed via `SELECT *` against the real `PMS_NZ_V2.Appointment.tblHealthLinkSession`: real columns are `InsertedAt`/`UpdatedAt`, no `EDIAccount`). This would have thrown on every single request against a real database - it only "worked" against the earlier synthetic `HekLegacyPmsDev` schema this project had fabricated for it. Fixed to call the real `[Appointment].[usptblHealthLinkSession_GetByGUID]` procedure (confirmed real, returns `EDIAccount` as a proc-computed field) plus a minimal separate `InsertedAt` read for the new 12-hour expiry rule (ADR-004, not part of legacy).
- **Verified `getVersion` and `getDeliveryOptions` end-to-end against the real `PMS_NZ_V2` database** with a genuinely fresh session row and real `EDIAccount` value (`"xxx"`) flowing correctly into the response per the real `PracticeEDI=="1"` branch.
- **`getData` reached the real concept-mapping pipeline and called the real `Hiso.uspGetPatient` procedure** - confirmed the concept dictionary and field-resolution engine both work against real data (previous session's `FirstName`/`LastName` test names were placeholders from a fabricated dictionary; real concept names are `Patient_FirstName`/`Patient_LastName` etc., confirmed from the real `[Hiso].[UspGetHisoConcepts]` output).
- **New architecture gap surfaced by real-DB testing, not fixable in this session**: `ILegacyPracticeConnectionResolver.ResolveAsync(session.PracticeId)` - used by every downstream repository (concept dictionary, ACC45, task, the 5 save builders) - expects the tenant registry's friendly `PracticeId` key, but `session.PracticeId` from the real session proc is the real numeric legacy `PracticeID` (e.g. `"933"`). Worked around for this test session by registering `933` directly in the tenant registry pointing at `PMS_NZ_V2`, but this is a structural mismatch between two different "PracticeId" concepts that predates this session and will recur for every other real practice - flagged, not fixed at the architecture level.
- **Hard blocker found, not yet resolved**: the real dynamic parameter-discovery procedure (`[Hiso].[USPGetProcedureParamList]`) - which `HisoConceptExecutor` depends on for every concept-driven procedure call - returns 0 rows for the `pms_nz` login regardless of procedure-name format tried. Root-caused to the same restricted-metadata-visibility pattern already seen on `sys.tables`/`sys.columns`/`sys.procedures` (confirmed via `sp_helptext` giving false "does not exist" for objects that demonstrably exist and execute fine - `pms_nz` lacks `VIEW DEFINITION`). This blocks real end-to-end completion of `getData` and every save operation. Zohaib will run `GRANT VIEW DEFINITION TO pms_nz;` on `PMS_NZ_V2` (and ideally `Indici_Master`/`DMS_PMS`) using an admin login - session paused pending that grant.
- **Zohaib granted `VIEW DEFINITION TO pms_nz`** and pasted the real legacy `Web.config`, unblocking real end-to-end verification:
  - **`getVersion`/`getDeliveryOptions`/`getData` fully verified against real production data.** `getData` resolved `Patient_FirstName`/`Patient_Surname` to a real patient's real name via the real `Hiso.uspGetPatient` procedure and the real `[Hiso].[USPGetProcedureParamList]` dynamic parameter-discovery engine - the actual legacy pipeline, not a stand-in.
  - **Found and fixed two more real bugs surfaced only by testing against the real DB**: (1) `HisoSessionRepository` was doing a raw `SELECT` against `CreatedAtUtc`/`EDIAccount` columns that don't exist on the real table - fixed to call the real `usptblHealthLinkSession_GetByGUID` procedure instead (see previous entry - re-confirmed here after the permission grant made deeper testing possible). (2) `Acc45DetailRepository`'s `BuildReferralTable` declared zero columns when no `accident.referral` group was present in the request, which SQL Server rejects outright for a TVP ("Structured types must have at least one field") - fixed to always seed a base column.
  - **Found a real design gap in `Acc45DetailRepository`, partially fixed**: `BuildDetailTable` built its TVP's columns dynamically from whatever fields happened to be present in the request XML, but the real `UDT_tblACC45Detail` type has a fixed 54-column shape - SQL Server rejected the mismatched TVP outright. Fixed by seeding the full real 54-column list (from Zohaib's Web.config) exactly like `Acc45DefinitionRepository` already did, then verified `saveContainer`'s definition-save + detail-save both pass through to the real procedure successfully. `BuildDiagnosisTable`/`BuildReferralTable` have the same latent bug (dynamic columns vs. the real fixed `UDT_tblACC45Diagnosis`/`UDT_tblACC45Referral` shapes - diagnosis confirmed needs 15 real columns via the SQL error, not the 2 present) - **not fixed this session**, since those two UDT column lists weren't in the Web.config section supplied. `saveContainer` is therefore build-verified and partially real-DB-verified (definition + detail save proven; diagnosis/referral sub-tables still need their real column lists).
  - Registered the real numeric practice ID (`933`, discovered from a real session row) in the tenant registry pointing at `PMS_NZ_V2`, alongside re-pointing `TEST-PRACTICE-HISO` itself - both needed because `session.PracticeId` downstream is the real legacy numeric ID, not the tenant registry's friendly key. This structural mismatch (two different "PracticeId" concepts) predates this session and will recur for every other real practice - still flagged, not fixed at the architecture level.
  - Added all real config secrets confirmed from the real Web.config to the (gitignored) local dev settings: `Hiso:PracticeEdi`/`UserId`/`Password`/`Url`, `Hiso:DmsPdfTypeId`/`DmsHtmlTypeId`, `Hiso:Acc45TaskTypeTypeId`/`TaskPriorityId`/`TaskStatusActive`/`TaskStatusCompleted`, `UDT_tblACC45Definition`/`UDT_tblACC45Detail`/`UDT_tblPatient`/`UDT_tblPatientConsult`/`UDT_tblPatientEmployerOrganisation`/`UDT_tblPatientProblem`/`UDT_tblRegisteredPractitioner`, `Hiso:ConceptMapping:QualifierList` (25 real values).
- **Still not verified this session**: `saveContainer`'s diagnosis/referral sub-tables (blocked on 2 missing UDT column lists, Zohaib chose to stop here for this session), `processAction`'s `"addTask"` branch (blocked on finding a real test SNOMED read-code - lower priority, not attempted after the ACC45 work), `processAction`'s `"save"` branch (5-builder pipeline - built and unit-tested but not yet run against the real DB), `getFormView` (built, not yet curl-tested against real data), `Indici_Master` connectivity (unreachable from this session's network - `PatientEmployerOrganisationBuilder`'s Country/City/Suburb/Designation lookups will gracefully no-op until reachable, by design).
- Build stayed green throughout (0 errors); full test suite re-run after every meaningful change, still 11/11.

### 2026-07-20 (getData rebuilt as a genuine port of the real legacy engine, against the full real HISO source - item 34 progress)

- Zohaib supplied the entire real legacy HISO Visual Studio project at `legacy-reference/Hiso/` - `FormSessionService.svc.cs` (the real WCF service, all 6 operations), the WCF contract, the real `ConceptMapper` (`HisoConceptDetail.cs`, `TypeConverter.cs`), every `*Builder.cs`, `Task.cs`, `DocumentHandler.cs`, `Mapper.cs`, `Utitlity.cs`, DAL files, and the real `Web.config`. Confirmed `AWSDoc.IndiciDMS` is a compiled DLL Zohaib will map in later, not source-portable.
- Ran three parallel research passes reading this source in full, then a fourth design pass, before writing any code - the approved implementation plan (file `swift-painting-blossom.md` from this session's plan-mode workflow) has the full detail.
- Resolved one direct conflict before proceeding: real source confirms `processAction`'s `addInvoice`/`launchForm` are genuinely empty, comment-only stubs - but an earlier project decision (Contract Design "Decision 4") had deliberately implemented them for real instead. Asked Zohaib directly rather than picking a side; **Zohaib: legacy fidelity wins for this compat surface** - the wire-compatible `processAction` endpoint (once built) will revert to legacy's no-op behavior; the existing REST `/acc45/...` endpoint can keep the real implementation as a separate surface.
- **Rebuilt `getData` as a genuine port**, not the earlier name-guessing approximation:
  - New real concept dictionary (`IHisoConceptDictionary`/`HisoConceptDictionary`) - calls the real `[Hiso].[UspGetHisoConcepts]` procedure, 10-minute cache, matching legacy's `MemoryCache` behavior exactly (genuinely DB-driven - confirmed no static/hardcoded dictionary exists anywhere in real source).
  - New real field-resolution engine (`IHisoRequestEngine`/`HisoRequestEngine`) - qualifier-ID/conceptName/conceptID priority matching with the real `MeasurementGroup`/config-driven `QualifierList` exceptions, group-concept XML cloning (one clone per DB result row), and the real `PrepareConceptsFromRequest` "empty group query" expansion logic, all ported from the real `HisoConceptDetail.cs`/`Mapper.cs` source, not reconstructed from documentation.
  - New real gating (`HisoGetDataOptions`) - only `IsDynamic=="1"` AND `formInstanceOperationMode=="N"` does real work, exactly like legacy; every other combination correctly returns an empty result, since that's what real legacy does too (not a gap this project introduced).
  - New MIME/PDF conversion hook (`IHisoMimeConverter`) - real routing/dispatch logic (PNG/BMP/HTML source detection) is built; the actual byte-level PDF render is Aspose-blocked (confirmed: legacy uses Aspose.Words specifically), same licensing gap already flagged for ACC45 (item 30) - placeholder returns original bytes unconverted, matching legacy's own graceful-degradation-on-failure behavior.
  - New AWS/second-node hooks (`IAwsDocumentService`, `ILegacyPracticeConnectionResolver.ResolveSecondNodeAsync`) - real calling-code orchestration confirmed from source is ready to wire up; both explicitly deferred pending Zohaib's DLL mapping and connection string.
  - Rewrote `GetDataRequest`/`GetDataResponse`/`GetDataQuery` to carry the real `FormData`/`FormMetaData` shape (XML content carried as a string field in the JSON envelope, since the real request/response bodies are XML-templated forms, not simple key-value data) instead of the earlier `Dictionary<string,object?>` simplification.
- **Verified end-to-end against real data**: added `db-scripts/05_HisoConceptDictionary.sql` (new - the real concept-dictionary table/procedure didn't exist in the earlier inferred schema build, since it was never part of the original schema inventory before the real source was supplied) with 3 real seed rows (`FirstName`/`LastName`/`DateOfBirth`, all mapped to the already-verified `Hiso.uspGetPatient_Demographics`). A real multi-field XML request through `POST /hiso/getData` returned all three fields correctly resolved and filled - the real engine, not a stand-in.
- Full test suite re-run after every meaningful change: still green (11/11) throughout.
- **Remaining scope for item 34** (large, explicitly not attempted yet): `getVersion`, `getDeliveryOptions`, `saveContainer`, `getFormView`, `processAction` each need the same real-source-grounded treatment - 8 more builder classes (5 Patient/Practitioner, 3 ACC45), `Task.cs`, `DocumentHandler.cs`, and the remaining `Mapper.cs` helper methods, all already read and detailed in the approved plan file, not yet ported into this codebase.

### 2026-07-20 (real getData compat endpoint built and verified — HISO data-retrieval wire compatibility gap, open item 34)

- Following the architecture clarification (previous entry), Zohaib pushed further with a concrete, specific ask: point out that the canonical REST demographics endpoints are not the same operation as HISO's real `getData` - different name, different path, different protocol - and asked how to verify the real `getData` flow, since that operation name is what an existing consumer is actually configured to call.
- Read the real documented shape directly (`HISO_doc.md` Section 3): SOAP operation, `.../getData`, request `{sessionKey, dataContainer}`, response `{GetDataResponseReturn:{dataContainer}}`, session-GUID-only auth (no separate bearer token), concept-mapping engine resolves a stored procedure per requested concept.
- **Discovered a real, previously-unflagged gap while building this**: `HisoSessionResolver`/`ResolveHisoSessionQuery` (session-GUID resolution, built in Block 1) had never actually been wired to any HTTP controller anywhere in the codebase - confirmed by grepping for call sites and finding none. Genuinely dormant infrastructure, not just an "unconfirmed inference" like most other flagged items.
- **Built `POST /hiso/getData`** (`src/Api/Features/Hiso/Controllers/HisoCompatController.cs`, `src/Adapters.Hiso/GetData/GetDataRequest.cs`/`GetDataResponse.cs`, `src/Application/Features/Hiso/Queries/GetDataQuery.cs`):
  - Matches the real request/response field names and wrapper (`GetDataResponseReturn`) exactly.
  - No `[Authorize]` requirement - resolves the session directly from the request body's `sessionKey`, matching the legacy operation's actual auth mechanism, unlike every other Block 2 controller which requires a pre-issued token.
  - Dispatches each `dataContainer` key as a concept name onto `Hiso.uspGetPatient_{ConceptName}`, reusing the already-ported `HisoConceptExecutor` (the real `getData`/`ExecuteHisoProcedure` engine) - flagged clearly that the real concept-to-procedure mapping table itself was never supplied, so this is the closest faithful reproduction available, not a rediscovered mapping.
- **Verified end-to-end for real**: created a dedicated test practice (`TEST-PRACTICE-HISO` → `HekLegacyPmsDev`) and a real session row in `[Appointment].[tblHealthLinkSession]`, without touching `TEST-PRACTICE-001`'s existing `PMS_NZ_V2` routing Zohaib is actively using. Added the missing `Hiso:ConnectionStrings:localhost:5299` config key the session-lookup mechanism needs (a separate routing path from the tenant registry, per ADR-007 - confirmed by reading `HisoSessionRepository.FindBySessionGuidAsync` directly). `POST /hiso/getData` with `{"sessionKey":"...","dataContainer":{"Demographics":{}}}` returned `200` with real data: `{"getDataResponseReturn":{"dataContainer":{"Demographics":{"FirstName":"Amy","LastName":"Mouse","DateOfBirth":"1985-05-05T00:00:00"}}}}`.
- Full test suite re-run: still green (11/11).
- **New open item 34**: HISO's `getData` now has real wire compatibility; KARO's and ERMS's data-retrieval operations have the identical gap and have not been addressed yet - same treatment needed once their real operation names/shapes are confirmed.

### 2026-07-20 (architecture clarification: "unified API, don't change any method" - confirmed as already-current approach, nothing changed)

- Zohaib raised a concern after seeing `HisoConceptExecutor` explained: "my goal was to never change any method just to make a unified api from all these 3 apis rest of things will call as it is." Worth resolving explicitly rather than assuming, since it could mean two very different things.
- Clarified the distinction directly: (1) the **database calls** (stored procedures, parameters, tables) are preserved faithfully - the project calls the exact same procedures the legacy systems did, never invented ones; (2) the **C# orchestration code** was necessarily rewritten, since the legacy code is .NET Framework 4.8/WCF/OWIN and cannot run inside this project's .NET 8 codebase - there is no way to literally reuse those old `.cs` files unchanged, only to read them and reproduce equivalent behavior in new code calling the same procedures.
- This was not a decision made mid-project - it was locked in at Implementation Planning (2026-07-19 entry, this file): **.NET 8, greenfield new repo**, explicitly because the legacy stack isn't reusable in ASP.NET Core 8.
- Asked directly whether the current approach (rewrite orchestration code, preserve database calls) matches what Zohaib wants, or whether a true pass-through gateway (forward every request to the three still-running old systems untouched, no reimplementation at all) was actually intended instead - **a fundamentally different architecture** that would require the old systems to keep running separately.
- **Zohaib confirmed: current approach is correct.** No architecture change needed. Recorded here so this doesn't resurface as unresolved confusion in a future session - the rewritten C# code calling the same stored procedures *is* "calling it as it is" in the sense that matters (same data, same business rules, same procedures); it could never have meant literally reusing the old .NET Framework files unchanged.

### 2026-07-20 (first endpoint verified against real production data — KARO demographics column mapping fixed; Swagger auth fixed along the way)

- Zohaib asked to test against `PMS_NZ_V2` (the real production-shaped database) instead of the dev scaffold, for three specific endpoints: HISO, KARO, and ERMS patient demographics.
- Checked `PMS_NZ_V2` directly first: confirmed (again) it has zero tables visible via `INFORMATION_SCHEMA.TABLES` under the `pms_nz` login. Flagged this before proceeding rather than assuming it would just work. Zohaib clarified: the database has real data/schema, the login just lacks `SELECT`/schema-visibility rights - it's scoped to `EXECUTE` on specific stored procedures only (matching exactly how the legacy C# code itself always calls the database, per `HSSDA.cs`/`DALHelper` - never raw `SELECT`). Confirmed this reasoning was right by testing via stored-procedure calls, not table queries.
- Repointed `TEST-PRACTICE-001`'s tenant-registry routing to `dbserver-local.fff`/`PMS_NZ_V2`. Zohaib supplied a real patient ID (2459731) to test with, since the login can't be browsed to find one independently.
- Minted three separate test JWTs (one per `originScope` - `Hiso`/`Karo`/`Erms`, ADR-003's structural origin-scope rule means one token only ever grants access to its own matching endpoint) and tested all three demographics endpoints directly against real data:
  - **HISO** (`Hiso.uspGetPatient_Demographics`): SQL error 2812 - procedure does not exist under that name. Confirmed wrong, not previously known for certain.
  - **ERMS** (`[HSS].[uspGetPatientData]`): same SQL error 2812 - also confirmed wrong.
  - **KARO** (`[HSS].[uspGetDemographics]`): different error entirely - a C#-level `ArgumentException` ("Column 'LastName' does not belong to table"), which only happens *after* SQL Server has already returned a real result row. That distinction (SQL-level "doesn't exist" vs. C#-level "wrong column name") is what pointed at KARO specifically as the one worth digging into further, rather than guessing.
- Added a temporary diagnostic to `DemographicsRepository.GetKaroAsync` to dump every real column name/value returned by the procedure (rather than guess at the right one), rebuilt, retested, and captured the real result: the procedure returns clean-named fields (`Given`, `Family`, `BirthDate`, etc.) alongside a second, differently-formatted set of similarly-named fields (`FirstName`, `Surname`, `DateOfBirth`, etc.) containing composite internal-reference strings like `554:1000111/1000310/1|&|LnB` - not usable values. The clean fields were the right ones.
- **Fixed for real**: `GetKaroAsync` now reads `Given`/`Family`/`BirthDate` instead of `FirstName`/`LastName`/`DateOfBirth`. Also discovered and fixed a related bug while at it: `DateOfEnrolment`/`EndEnrolmentDate` come back as **empty string, not `DBNull`**, when unset - the original `is DBNull` check would have thrown a `FormatException` on real data. Added `ParseOptionalDate` to handle both cases. Removed the temporary diagnostic once the fix was confirmed.
- **Verified**: `GET /patients/2459731/demographics/karo` now returns a genuine `200` with real data from `PMS_NZ_V2` - `{"patientId":2459731,"practiceId":"TEST-PRACTICE-001","firstName":"LnB","lastName":"WMKCU","dateOfBirth":"2019-01-12","dateOfEnrolment":null,"endEnrolmentDate":null}`. This is the **first endpoint in this project fully verified against real production data**, not a dev scaffold.
- **Separate fix found and applied along the way**: Zohaib tried testing via Swagger UI and hit `401 Unauthorized` with no way to attach a token - `AddSwaggerGen()` had no security scheme configured at all, so Swagger's "Authorize" button didn't exist. Added a proper JWT Bearer security definition so Swagger can actually be used for manual testing going forward, not just curl.
- Full test suite re-run after all changes: still green (11/11 across all test projects).
- Open item 28 updated to reflect the first real verification, with the specific caveat that most of the ~35 other inferred procedure names remain unverified - this doesn't close the item, it makes it partially real for the first time.

### 2026-07-20 (KARO confirmed to share ERMS/COL's SaveInvoice method — item 17's last open thread closed)

- Zohaib confirmed directly: "karo also use same method as erms use" - KARO's SaveInvoice is not a separate implementation with its own undiscovered field list; it shares the same underlying method as ERMS/COL's `SaveInvoice` (already fixed against the real `COLController.cs`/`HSSDA.cs` source earlier this session).
- Consistent with an already-documented project fact (§2): KARO and ERMS share one DAL codebase (identical `[HSS]`-schema procedure names, identical hardcoded secrets, identical dormant-module code) - this is the same pattern applying to SaveInvoice specifically, not a new/surprising claim.
- No code change needed - `InvoicesRepository.SaveAsync`'s field list and procedure mapping (fixed earlier this session against real ERMS/COL source) already covers this shared method, so it applies to KARO callers too without modification.
- Attempted to invoke the `docs-driven-workflow-skill` explicitly for this update (per Zohaib's standing instruction to call project skills explicitly rather than follow them from habit) - the `Skill` tool returned "Unknown skill" for both `docs-driven-workflow-skill` and `docs-driven-workflow` (the name in the file's own frontmatter). The skill file exists at `.claude/skills/docs-driven-workflow-skill.md` but is not registered as invocable in this environment's skill loader. Followed its written workflow manually instead (already had the file's content loaded from a direct read earlier in this session) and flagged the tool-registration gap to Zohaib rather than silently working around it or pretending the call succeeded.
- Open item 17 now has no remaining caveat - closes cleanly.

### 2026-07-20 (tenant registry schema reconciliation — open items 24/31 closed, real bug found and fixed)

- Zohaib asked to tackle the tenant registry schema reconciliation (open items 24/31) next.
- Re-checked the EAD and the full ADR-001 text directly before doing anything - confirmed (again) that no source document anywhere gives a literal registry column list, only ADR-001's prose. Since there is nothing to "confirm against," closed item 24 by signing off Block 1's existing schema as the deliberate design, per that item's own stated resolution path.
- **While investigating item 31, found this wasn't just a documentation mismatch - it was a real, undiscovered bug.** `PracticeAdminRepository`'s old mapping set `DbName = input.DatabaseServerId` - the *same* value as `DbServerHost`. Since `ILegacyPracticeConnectionResolver` builds connection strings as `Server={DbServerHost};Database={DbName};...`, registering any real practice through `/admin/practices` would have produced a connection string pointing at a database with the same name as its own server hostname - broken for every real case, since a database name is never identical to its server's hostname. This had never been caught because `/admin/practices` had never actually been exercised end-to-end in this project.
- **Fix applied**: `PracticeInput`/`Practice` (`src/Contracts/Admin/`) now carry three explicit fields - `DbServerHost`, `DbName`, `SourceSystem` - instead of one collapsed `databaseServerId`. `SourceSystem` reuses the existing `OriginScope` enum (`Hiso\|Karo\|Erms\|Col`, already defined for ADR-003's token scoping) rather than inventing a new type or leaving it as a free-text `"Unknown"` default. `PracticeAdminRepository.RegisterAsync`/`UpdateAsync`/`GetAsync` all map the three fields directly now, no duplication.
- OpenAPI spec updated to match: `PracticeInput`'s `databaseServerId` replaced with `dbServerHost`/`dbName`/`sourceSystem` (enum-constrained), bumped to 1.1.4 with an `x-changelog` entry explaining the reasoning (a new-design correction, not a legacy-compat concern, since no legacy system ever had this admin endpoint).
- Validators (`PracticeAdminValidators.cs`) updated to validate the new required fields.
- Solution rebuilt clean (0 warnings/errors); full test suite green (Api.IntegrationTests 3/3, Application.UnitTests 7/7, Infrastructure.UnitTests 1/1).
- **Net effect**: items 24 and 31 both closed - 24 by explicit new-design sign-off (nothing to reconcile against), 31 by fixing an actual defect that would have broken every real practice onboarded through this endpoint, not merely documenting the mismatch as before.

### 2026-07-20 (rate-limit thresholds — open item 18 closed by formalizing the existing generous defaults)

- Zohaib asked to tackle open item 18 (rate-limit thresholds) next. Investigated before proposing anything: confirmed no source document anywhere in the project has quantitative traffic figures - the SRS explicitly says "cannot be confirmed from analysis" for latency/throughput/SLA numbers, with only a qualitative "10,000 concurrent users" target and no request-rate breakdown. Rate limiting itself is net-new (legacy systems had none at all).
- Rather than fabricate a "final" number with nothing to base it on (would violate the standing "never invent unconfirmed business values" rule), asked Zohaib directly via a multiple-choice question: formalize the existing generous defaults as intentional, provide real numbers, or tighten to a conservative engineering-judgment default now. **Zohaib chose to formalize the existing generous defaults.**
- This aligns with a decision the Contract Design doc had already made (Section 14): ship generous/effectively-off limits at launch, tighten only once real monitoring data exists - so "resolving" this item didn't require picking a number at all, just confirming the current placeholder (10,000 requests/60s per IP) is the deliberate Day-1 value, not an unconfirmed guess awaiting a real one.
- Updated `RateLimitOptions.cs`'s doc comment to state this plainly - no functional/numeric change, since the values were already reasonable for "generous at launch."
- Solution rebuilt clean: 0 warnings, 0 errors.
- **Still true**: there is no real number to tighten to yet, since no production monitoring data exists. This item is closed as "confirmed intentional," not "final production values decided" - revisit once real traffic data exists.

### 2026-07-20 (ACC45 Aspose placeholder reconfirmed, marked with an explicit TODO)

- Zohaib reconfirmed the existing decision on open item 30: keep the JSON placeholder, do not substitute a different PDF/rendering library for Aspose, keep the save/DMS sequencing intact, and wait for a valid company Aspose license before implementing the real rendering step.
- No behavior change - `Acc45Repository.SaveFormAsync`'s sequencing (render-to-DMS, then persist the ACC45 definition with the returned DMS GUID, per HISO-BR-12) was already correct and untouched.
- Added an explicit `// TODO(Aspose): ... // END TODO(Aspose)` bracket directly around the rendering call site, so the exact scope of the future change (bytes/extension/description passed to `DmsDocumentService.AddDocumentAsync`) is unambiguous and isolated from the surrounding logic, per Zohaib's request to "clearly mark the rendering step with a TODO so it can be completed later without changing the surrounding workflow."
- Solution rebuilt clean: 0 warnings, 0 errors.

### 2026-07-20 (practiceId-in-legacy-login gap (item 32) scaffolded as a config toggle, off by default)

- Zohaib asked how auth actually worked in legacy, and separately clarified two example values ("901 is the practiceid and FZZ999-B is the other key") while asking what was actually needed. Re-read `KARO_HSS_doc.md` and `ERMS_doc.md`'s Authenticate sections precisely before answering: neither system's *request* payload ever includes practiceId, but both systems' *responses* do (`"practiceId": "demo"` / `<PracticeId>demo</PracticeId>`) - meaning the old backend resolved it server-side from something, most likely `pho` (KARO) or an internal username lookup, and that resolution logic was never part of the supplied source.
- Zohaib's direction: "leave it configurable and turn off, we will check this later it should work as it was working in legacy."
- **Built `LegacyPracticeResolutionOptions`** (`src/Application/Common/Options/`) - an ADR-008-style config toggle (`Auth:LegacyPracticeResolution:Enabled`, default `false`), matching the same pattern already used for `Auth:Enabled` and rate limiting.
- **Wired into `AuthenticateCommandHandler`**: when a legacy compat login has no practiceId (always true today, since none of the translators supply one) and the toggle is enabled, looks up `Auth:LegacyPracticeMappings:{username}` via the existing `ISecretProvider` abstraction and uses it if found. While the toggle is off (the shipped default), behavior is byte-for-byte unchanged from before this existed.
- Keyed by **username**, not `pho` - flagged explicitly as an assumption, not a confirmed fact, since Zohaib's own example pair (`901`/`FZZ999-B`) doesn't match KARO's documented 4-letter `pho` format (`NBPH`), so the real lookup key is still unconfirmed. Real mapping data plus confirmation of the key type is still needed before this can be safely turned on.
- Two new unit tests added (`Application.UnitTests/Auth/AuthenticateCommandHandlerTests.cs`) proving both toggle states: disabled leaves practiceId empty and never calls the secret provider; enabled resolves and uses the looked-up value. Full test suite green (Application.UnitTests now 7/7, all projects passing).
- **Net effect**: item 32 is no longer a silent gap - it's a documented, inert, toggle-gated capability. Nothing about current runtime behavior changed (still empty practiceId on compat logins, exactly as before), matching "turn off, we'll check this later."

### 2026-07-20 (billing:write scope requirement removed — open item 29 closed by stakeholder override)

- Zohaib pushed back on open item 29 directly: "implement as it is as legacy system was working i do not understand why you are asking for billing rights."
- Before applying the change, checked what the `billing:write` requirement was actually based on, since removing an authorization control is security-relevant and the project's standing practice is to verify rather than assume. Traced it to SRS §12.2, which recommends a distinct financial-write authorization scope and cites `erms/SecurityAnalysis.md` SEC-04 as its evidence. Read SEC-04 directly: it is actually a wildcard-CORS finding (`COLController` decorated `[EnableCors(origins: "*")]`), not a finding about missing authorization scoping. Cross-checked against the real legacy source (now supplied, `legacy-reference/controller/COLController.cs`): `SaveInvoice` is gated by `HSSDA.InsertAndValidateToken(...)`, the exact same session-token validation every other ERMS/COL endpoint uses - there was never a distinct "billing" permission tier in the legacy system at all.
- **Conclusion presented to Zohaib before acting**: the `billing:write` requirement was a new-for-this-platform recommendation from the SRS phase, not a legacy behavior being preserved, and the SRS's own cited evidence for it doesn't actually support requiring a distinct scope. Zohaib's direction to match the legacy system's real behavior is consistent with what the source code shows, not a request to reintroduce a known vulnerability - so applied without further pushback.
- **Change applied**: removed `[Authorize(Policy = AuthorizationPolicyNames.BillingWrite)]` from `InvoicesController.Save` - it now relies on the same `ResourceScoped` policy (`ResourceScopedControllerBase`) every other Block 2 write endpoint already uses (valid token, scoped to the requested patient). The `BillingWrite` policy constant and its `Program.cs` registration were left in place, unused, rather than deleted, in case a real scope-granting design is wanted in the future.
- OpenAPI spec updated to match: removed the `bearerAuth: [billing:write]` override on `POST /patients/{patientId}/invoices` (now falls back to the global `bearerAuth: []` default like other endpoints), bumped to 1.1.3 with an `x-changelog` entry explaining the reasoning.
- Solution rebuilt clean: 0 warnings, 0 errors.
- **Net effect**: the invoices endpoint is now both correctly wired to the real legacy stored procedure/field list (item 17, previous entry) and actually callable by any properly-scoped caller (item 29) - both halves of what was blocking billing are now closed.

### 2026-07-20 (billing/invoices rebuilt against real ERMS/COL source — open item 17 closed)

- Zohaib pointed to two files that had been supplied but not yet connected to the billing gap discussion: `legacy-reference/DAL/HSS/HSSDA.cs` and `legacy-reference/controller/COLController.cs`. Together they give the complete, real picture of the endpoint previously logged as "KARO's SaveInvoice" (§3 item 17) - it's actually **ERMS's COL/Pegasus integration**'s `POST /col/saveinvoice`, not KARO.
- `COLController.SaveInvoice()` deserializes a `SaveInvoice` JSON model and calls `HSSDA.InsertUpdateService(...)`, which executes `[OnlineClaim].[uspInsertUpdateService]` - the previously-guessed procedure name (`[HSS].[uspSaveInvoice]`) was wrong, now corrected.
- **Real field list confirmed**, including three fields the earlier inferred `InvoiceInput` contract had silently omitted: `Description`, `AccountHolderId` (`AccountHolderID` in source), `ServiceProviderType`. Added to `InvoiceInput`/`Invoice` (`src/Contracts/Billing/`) and to the OpenAPI spec (bumped to 1.1.2, x-changelog entry added) per the standing "don't drop what the old system implemented" rule.
- **Two confirmed legacy quirks carried forward as-is, not invented**: `masterServiceName` is hardcoded to the literal `"COL"` inside the legacy controller (never sent by the caller) - preserved as a hardcoded value in `InvoicesRepository.SaveAsync`, not exposed as a request field. `locationId` is likewise always empty/`DBNull` in the legacy call - preserved the same way.
- **One field genuinely renamed at the DAL layer, not the public contract**: the legacy DAL's `masterServiceName`/`subServiceName`/`subServiceCode` split doesn't 1:1 match this contract's `ServiceCode`/`ServiceName` naming - confirmed the real mapping (`ServiceName`→`subServiceName`, `ServiceCode`→`subServiceCode`) and wired it correctly in the repository, without renaming the already-locked public OpenAPI field names.
- **Kept the platform's own idempotency design intentionally, did not revert it**: checked the Contract Design doc first (Section 12, Decision 3, FR-IDEM-01) before touching anything idempotency-related, since the existing `FindByNaturalKeyAsync`-based approach turned out to be a *deliberate, already-approved* replacement for the legacy `-3` magic-number convention, not a gap. Left that mechanism in place; only added the legacy procedure's own `-3` return code as a defense-in-depth signal on top of it (`InvoicesRepository.SaveAsync` now returns a `WasDuplicate` flag alongside the invoice).
- **Confirmed correct on the platform's own terms, not reverted to legacy**: the canonical endpoint keeps its existing REST status codes (200/201/400/401/403), RFC 7807 error shape, and `billing:write` scope-based auth - the legacy endpoint's always-200/raw-error-text/session-token behavior was not cloned in, since that's a deliberate, already-documented canonical-vs-compat-endpoint design split (open item 27), not something this specific fix was asked to change.
- **`patientId`/`encounterId` threading fixed**: `InvoicesController` now passes `CurrentScope.EncounterId` (from the caller's resource-scoped token, ADR-003) into `SaveInvoiceCommand`, since the real DAL call needs an `@pAppointmentId` value that the earlier version never supplied.
- Solution rebuilt clean (0 warnings/errors) after the change.
- **Known follow-up, not yet done**: `db-scripts/03_LegacySchema_Build.sql` (the dev-only `HekLegacyPmsDev` schema, open item 28) still defines the old, now-superseded `[HSS].[uspSaveInvoice]` stored procedure rather than `[OnlineClaim].[uspInsertUpdateService]` - the local dev database and the actual application code are now out of sync for this one endpoint specifically. Not fixed in this pass since it wasn't asked for; flagged so it isn't silently forgotten if billing gets end-to-end tested against that database later.
- **Still open**: KARO's own SaveInvoice implementation (if a separate one exists) was never supplied - this closes the ERMS/COL half of item 17 only.

### 2026-07-20 (Docker Desktop turned on — `docker compose up` attempted, blocked by a host-level network issue, not a code defect)

- Zohaib turned Docker Desktop on; attempted the "not yet done" `docker compose up` end-to-end verification flagged since Block 0/1 (Phase 12 change-log entry, 2026-07-20 earlier in this file).
- Created a local, gitignored `.env` (`SA_PASSWORD`/`AUTH_JWT_SIGNING_KEY` dev-only values) and ran `docker compose build api` — succeeded cleanly (multi-stage build, `dotnet restore`/`publish`, 0 errors).
- `docker compose up -d` — both containers start; `sqlserver` (`mcr.microsoft.com/mssql/server:2022-latest`) reaches `healthy` reliably. `api` crashes on every start with `SqlException ... TCP Provider, error 40` while running its startup tenant-registry migration (`TenantRegistrySeeder.SeedAsync` → `Program.cs:172`), unable to reach `sqlserver` at all.
- **Extensive isolation performed, all ruled out as the cause**: DNS/hostname resolution (confirmed working via `getent hosts`), raw TCP+login to the exact same target (confirmed working via `sqlcmd` from within the `sqlserver` container over its own network alias, and via `nc` from an independent third-party `busybox` container on the same bridge network - both succeed instantly), TLS/encryption settings (`Encrypt=False` tried, no change), IPv4/IPv6 socket preference (`IPAddressPreference=IPv4First` tried, no change), connecting by the container's raw IP instead of DNS name (no change), and connecting via `host.docker.internal` instead of the internal bridge entirely (no change - every variant produces the identical error with `ClientConnectionId` all-zeros, meaning TCP itself never establishes). A same-image control test (installing `nc` inside the `api` container to test with a non-.NET tool) couldn't run because that container's `apt-get` also couldn't reach the internet, while sibling containers' intra-bridge traffic works fine - pointing at something specific to the built `api` image/container or a host-level Docker network-isolation feature (Docker Desktop's Enhanced Container Isolation, a corporate VPN/EDR agent, or a WSL2 vEthernet quirk), not an application bug.
- Reverted the exploratory `Encrypt=False` change to `docker-compose.yml` (didn't fix anything, no reason to keep it). Tore the stack down (`docker compose down`) rather than leave it running broken.
- **New open item 33** added rather than silently marking this "done" - `docker compose up` end-to-end verification is still not achieved, and unlike every other open item in this project, the likely next step is host-side investigation (Docker Desktop settings, AV/EDR/VPN filtering) rather than a code change, since the exact same target is reachable from other containers on the same host/network using non-.NET tools.
- Note: the app itself has already been verified working correctly against real data outside Docker, on the host directly against `HekLegacyPmsDev` (previous change-log entry) - this gap is specific to the containerized network path, not the application logic.

### 2026-07-20 (real database wiring — HekLegacyPmsDev built, seeded, and verified end-to-end)

- Following Block 2/3 completion, shifted focus to proving the codebase against real data rather than just inferred schema. Iterated through several routing options with Zohaib before landing on the final approach (full detail in `AI_USAGE_LOG.md`, which is now the authoritative step-by-step record for this phase): tried the supplied `PMS_NZ_V2` connection string first (no DDL rights on the `pms_nz` login — generated a standalone idempotent EF migration script instead of failing silently); a second supplied connection string (`ClinicBooking`) turned out to belong to an unrelated existing application — checked `INFORMATION_SCHEMA.TABLES` before touching anything, flagged it, did not proceed; created a dedicated `HekTenantRegistry` database instead and verified `/health/ready` against it for the first time.
- Zohaib then asked to catalog every stored procedure/table/parameter the codebase references, for a *future* dedicated database, explicitly not to be built yet — produced `db-scripts/REQUIRED_SCHEMA_INVENTORY.md` (documentation only, grepped from `src/Infrastructure/Legacy/**/*.cs`, no DB touched).
- Zohaib then reversed course twice in quick succession: first to proceed against `PMS_NZ_V2` now, then (mid-turn) to instead build the full schema on a **new local database** and keep `PMS_NZ_V2` configured separately for later/other use — confirmed via AskUserQuestion before acting.
- **Built `HekLegacyPmsDev`**: new database on `localhost`; `db-scripts/03_LegacySchema_Build.sql` (new, idempotent T-SQL — 6 schemas, ~20 tables, ~50 stored procedures, every name taken directly from `REQUIRED_SCHEMA_INVENTORY.md`) executed clean (90 batches, 0 errors); `db-scripts/04_SeedDummyData.sql` (new) executed clean, seeding patient 1001/encounter 5001/practice `TEST-PRACTICE-001` across the domain tables plus the `Hiso.ProcedureParams` dictionary the HISO concept-engine needs at runtime.
- Redirected `HekTenantRegistry.Practices` so `TEST-PRACTICE-001` now routes to `localhost`/`HekLegacyPmsDev` instead of `dbserver-local.fff`/`PMS_NZ_V2`; added the matching `localhost` credential entry to the gitignored local config; `PMS_NZ_V2`'s credentials remain configured, untouched.
- **New open item (32):** while preparing a token to test with, found that the legacy compat auth translators (`HssAuthenticateTranslator` and siblings) have no `practiceId` field anywhere in their documented legacy request shapes, so the real `/karo/authenticate` flow as shipped cannot mint a token with a usable `practiceId` claim — every downstream tenant-registry lookup would fail. Not fixed (needs Zohaib's direction on where `practiceId` should come from for these callers); worked around for this verification only with a hand-crafted JWT signed with the app's own dev signing key.
- **Verified for the first time in this project:** `GET /patients/1001/demographics/karo` against the running app, with the hand-crafted token, returned `200` with the exact seeded row (`Amy Mouse`, DOB `1985-05-05`, enrolled `2015-03-01`) — proving the complete chain (JWT auth → authorization → MediatR → repository → `ILegacyPracticeConnectionResolver` → tenant-registry lookup → `LegacyDbExecutor` → stored procedure → response) works correctly against a real (if newly-built) SQL Server database. Stopped the background process after confirming, consistent with practice throughout this project.
- **Caveat carried into open item 28**: this proves the code is internally consistent with its own inferred schema, not that the inferred schema matches the real, live Indici PMS/HSS schema — `HekLegacyPmsDev` was built from the same inferred names item 28 already flagged. Downgraded item 28's framing from "no way to test this yet" to "tested against itself, still unverified against the real thing."
- **Key files:** `db-scripts/03_LegacySchema_Build.sql`, `db-scripts/04_SeedDummyData.sql`, `db-scripts/REQUIRED_SCHEMA_INVENTORY.md` (all new), `AI_USAGE_LOG.md` (new this phase overall, now holds the full granular step log for all database-wiring work), `src/Api/appsettings.Development.local.json` (gitignored, updated — not reproduced here).

### 2026-07-20 (Block 2 — all 18 domain endpoint groups built; Block 3 contract re-verification complete)

- Executed Block 2 of the Day-1 build sprint per the Implementation Plan's priority order, immediately following Zohaib's "start block 2" instruction. All 18 domain groups (42 canonical OpenAPI operations) built, following the Clean Architecture pattern established in Block 0/1 (Contracts DTO → Application command/query/handler/validator via MediatR → Infrastructure repository → Api controller extending the new `ResourceScopedControllerBase`), and verified via incremental builds after every group (all green, 0 warnings/errors) plus a final full solution build + 9/9 passing tests.
- **Foundational discovery, addressed head-on rather than deferred repeatedly**: HISO's "getData" concept-mapping engine (`DBMessages.cs`, set aside in the prior session as "leave for Block 2") turned out to be load-bearing infrastructure for the majority of HISO-sourced read endpoints (demographics, notes, conditions, medications, lab results, observations all list "HISO getData" as their legacy source), not a niche ACC45-only concern. Ported its core (`ExecuteHisoProcedure`/`GetParamList`/`MapParamList`) into `src/Infrastructure/Legacy/Hiso/HisoConceptExecutor.cs` as new, live (not dormant) shared plumbing, reconstructing the `HealthLinkSession`/`HisoRequest`/`DynamicParam` types directly from their observable usage in the real supplied source (not invented from nothing) - see ADR-012 (to be updated) and open item 28 for the full flagged-inference detail. The AWS-enrichment branch is ported but throws/logs rather than executing, since `AWSDoc.IndiciDMS`'s source was never available even to the original Phase 1 analysis.
- **New Block 2 foundation** (before any domain group): `ILegacyPracticeConnectionResolver` (combines Block 1's tenant registry routing with `ISecretProvider` credentials into an actual ADO.NET connection string - the missing link between "know which server" and "actually connect"), a convention-based `AddInfrastructureRepositories()` DI scanner (avoids one manual `Program.cs` registration line per domain group), `ListResponse<T>` (the canonical `{items:[...]}` envelope), and `IIdempotencyStore` (in-memory Day-1 implementation, explicitly flagged as not correct across the multi-instance load-balanced fleet ADR-005 targets - a hardening-phase item, not silently presented as production-ready).
- **All 18 groups delivered**: Patient Demographics (4 legacy-shaped endpoints), Clinical Notes, Conditions (idempotent per KARO-BR-12), Medications (regular/prescribed view), Lab/Radiology Results (list+detail x2), Documents (idempotent per ERMS-BR-19-style natural key; Aspose rendering flagged not implemented, see open item 30), Observations (KARO-BR-14 at-least-one-field enforced), ACC45 (all 7 endpoints incl. `addInvoice`/`launchForm`/static-mode implemented for real, not stubs, per Decision 4/5 - this satisfied domain group #18's dead-code-paths requirement too, no separate work needed), Encounter Summary Templates (KARO-BR-11 typed field validation against schema), Tasks (HISO-BR-19/20 concept-name-resolved subject + configured status IDs), Recalls, Screening (real auth+persistence replacing the legacy no-op, per FR-AUTH-04), Providers, Practice/Session Context (COL-gated), Billing/Invoicing (idempotent, `billing:write`-scoped - see open item 29 for the scope-granting gap), Tenant/Practice Administration (the actual write path onto Block 1's read-only registry), and Health/Diagnostics (confirmed Block 0's `/health` already covers the KARO/ERMS Ping equivalence - fixed its response body to the exact `{"status":"ok"}` OpenAPI shape, previously plain text).
- **Block 3 (contract re-verification) executed**: every controller's route/verb was extracted and cross-checked against the OpenAPI spec's 42 operations - full path-for-path, verb-for-verb match, no missing or extra canonical endpoints. One real, deliberate drift was found (`POST /auth/token` returns `501`, not the spec's literal `200`/`401`, per ADR-012 Decision 4/open item 26) and **corrected in the spec itself** rather than left silently mismatched: `openapi.yaml` bumped to 1.1.1 with an `x-changelog` entry and a documented `501` response added to `/auth/token`.
- **New standing pattern across every Block 2 repository**: since no live database/schema access exists anywhere in this project, every repository's stored-procedure and result-column names are inferred from the two confirmed naming conventions (`[HSS].[uspX]` for KARO/ERMS, `Hiso.uspGetPatient_X` for HISO) and flagged explicitly in a doc comment, never presented as confirmed. Tracked as one consolidated open item (28) rather than one per repository, since it's the same caveat repeated ~18 times.
- **Two new, real gaps surfaced and flagged rather than silently worked around**: `billing:write` scope is checked correctly but never granted by anything (open item 29 - the invoices endpoint currently rejects every caller until scope-granting is designed); Aspose-based ACC45 form rendering is not implemented since Aspose is commercially licensed software unavailable in this environment (open item 30 - the DMS-then-persist *sequencing* HISO-BR-12 requires is implemented, only the rendering step itself is a placeholder).
- **Day 1 build sprint is now functionally complete** per the Implementation Plan's exit criteria (§4): solution builds and runs; every OpenAPI endpoint has a real, non-stubbed handler; auth/tenant-routing/rate-limiting are wired end to end (Block 1); the confirmed SQL injection is fixed (`DMSDA.cs`); CI is green. Explicitly still a *complete draft*, not production-ready - the post-sprint hardening stage (test suite, security verification, load testing against real data, staged ADR-008 rollout) has not started, and cannot meaningfully start until open item 28 (schema verification) is addressed.

### 2026-07-20 (DBMessages.cs decision: leave for Block 2)

- Presented with the finding that `DBMessages.cs` contains no confirmed injection and is tightly coupled to undocumented HISO concept-mapping types, Zohaib decided: **leave it for Block 2**, rather than have stub types invented now. Open item 23 updated to reflect `DMSDA.cs` closed / `DBMessages.cs` deferred (not "blocked" or "flagged pending" — a settled decision). When Block 2 builds the ACC45/HISO domain group, `DBMessages.cs`'s `ExecuteHisoProcedure`/`GetParamList`/`MapParamList` logic should be ported then, alongside the real `HealthLinkSession`/`HisoRequest`/`DynamicParam` types Block 2 will define for that domain group anyway.

### 2026-07-20 (Block 1 follow-up — DMSDA.cs ported and fixed; DBMessages.cs flagged back to Zohaib)

- Zohaib supplied the real legacy source for both dormant-module files at `legacy-reference/DAL/DMS/DMSDA.cs` and `legacy-reference/DAL/MHNHL7/DBMessages.cs` (outside `src/`, not part of the buildable solution — reference-only, per the agreed plan).
- **`DMSDA.cs` read, then ported** to `src/Infrastructure/Legacy/Dormant/Dmsda/DmsDocumentService.cs`. The confirmed injection was in `UpdateInboxFolderDocuments` — a `private`, never-called method (dead code within its own class in the original, consistent with "dormant" at the method level too) that built `CommandText` via string concatenation of a caller-supplied `Guid` and `InboxFolderItemID`. Fixed via full parameterization. The rest of the file (`HL7SaveInbox` overloads, `TaskPathLabInsert`, `GetOrganizationByEDI`, `SaveDMS`, `AddDocument`, `GetDocumentTypeID`, `GetColumnMaxValue`, `GetDMSData`, `UpdateData`, `UpdateAllData`) was already parameterized in the original and ported faithfully. Since the original `DALHelper` class it called wasn't part of the supplied source, a minimal fresh equivalent (`LegacyDbExecutor`) was written — flagged in its own doc comment as a fresh re-implementation of the same call shape, not a port of unseen code. Connection strings (`ConnMHNPMS`, `ConnMHNDMS`) and the `DMSDocTypes`/`SqlCommandTimeOutInSeconds` app-settings now resolve via `ISecretProvider`/`IOptions<LegacyDmsOptions>` instead of `ConfigurationManager`. Registered in DI (`Program.cs`), not wired to any controller/endpoint — still dormant, per the stakeholder's "keep, don't delete" decision.
- **Known, flagged gap**: the file's TIFF/BMP→PDF conversion (`ConvertImageBytes`) uses `System.Drawing.Common`, which is Windows-only since .NET 6 (throws `PlatformNotSupportedException` on Linux). Ported faithfully with a `[SupportedOSPlatform("windows")]` guard and an explicit `OperatingSystem.IsWindows()` check at the call site — on Linux containers (ADR-005) this now explicitly skips conversion and stores the original bytes, rather than relying only on the legacy code's own catch-all fallback. Also required upgrading from the legacy code's PdfSharp 1.x-era API (`XImage.FromGdiPlusImage`, implicit int→`XUnit`) to PdfSharp 6.1's current API (`XImage.FromStream`, explicit `XUnit.Point`), since the exact legacy package version's API is gone from current NuGet — functionally equivalent, adapted API surface, flagged in code comments.
- **New unit test** `Infrastructure.UnitTests/Legacy/DmsDocumentServiceTests.cs` proves the fix: calling the ported method with a SQL-metacharacter payload (`'; DROP TABLE ...; --`) as the `Guid` argument fails only at the connection level (no reachable test DB in this environment), never with a SQL syntax error — the practical proxy available here for "this can no longer be used to inject SQL." All 9 unit tests across the solution pass (0 failures).
- **`DBMessages.cs` read, NOT ported — flagged back to Zohaib rather than guessed at.** Two findings, both material: (1) as supplied, the file contains **no string-concatenated SQL** — every database call already goes through parameterized `SqlParameter` lists via the same (unsupplied) `DALHelper` class, so the "confirmed SQL injection" framing from the SRS/ComparisonReport doesn't match what's actually in this file (the one soft risk pattern present — using a caller-influenced `ProcedureName` string as the executed stored-procedure identifier without an allow-list — is a different, lesser concern, not string-concatenated dynamic SQL); (2) the file is deeply and directly coupled to HISO's live concept-mapping engine (`ExecuteHisoProcedure`, `GetParamList`, `MapParamList` against `HealthLinkSession`/`HisoRequest`/`DynamicParam`/`AWSDoc.IndiciDMS`/`Hiso.ConceptMapper`) — none of these supporting types exist anywhere in this repo yet; they're squarely Block 2's ACC45/HISO domain-endpoint territory (Implementation Plan Block 2 group #8/#9), not something Block 1's dormant-module cleanup should invent placeholder shapes for. Porting it now would mean fabricating five-plus undocumented type shapes, directly conflicting with the project's standing "never invent an endpoint/type shape that isn't in the docs" rule. **Open item 23 updated to reflect this split** rather than left stale — `DMSDA.cs`'s half of SRS Phase A hard-blocker item 4 is closed; `DBMessages.cs`'s half is open pending Zohaib's direction (defer to Block 2, where the real supporting types will exist naturally, vs. build stubs now).

### 2026-07-20 (Phase 12 — Development, Day-1 Block 0 + Block 1 built and verified)

- Executed the Day-1 build sprint's Block 0 (scaffolding) and Block 1 (security core) per the agreed Implementation Plan sequencing, stopping for Zohaib's sign-off before Block 2 (18 domain endpoint groups), exactly as instructed.
- **New git repo** created at the project root: `HekCoreApi.sln`, 8 `src/` projects (`Domain, Contracts, Application, Infrastructure, Adapters.Hiso, Adapters.Karo, Adapters.Erms, Api`) + 5 `tests/` projects, Clean Architecture dependency graph enforced via `<ProjectReference>` edges matching architecture-rules skill exactly (Presentation→Application→Domain only, Infrastructure/Adapters.* never referenced by anything except `Api`, Domain has zero dependencies).
- **Block 0 delivered:** `Directory.Build.props`/`Directory.Packages.props` (nullable + warnings-as-errors + central package versions), `ISecretProvider`/`EnvironmentVariableSecretProvider` vertical slice (direct replacement for the hardcoded `tcpepms*1` pattern), Serilog + `CorrelationIdMiddleware`, `/health` (liveness) + `/health/ready` (SQL-dependent readiness) via built-in ASP.NET Core health checks, `src/Api/Dockerfile` + `docker-compose.yml`/`.override.yml`/`.env.example`, `.github/workflows/ci.yml`. **Verified:** solution builds clean in Debug and Release with 0 warnings/errors; ran the built `Api.dll` directly against no SQL Server and confirmed `/health` returns 200 immediately while `/health/ready` returns a non-200 error — the liveness/readiness distinction genuinely does something. **Docker itself could not be exercised** — Docker Desktop's service was stopped in this environment; `docker compose up` verification is a manual step still owed before this is treated as fully proven, not something I fabricated a result for.
- **Block 1 delivered:** `Contracts` project (Error/TokenRequest/TokenResponse/ResourceScope/OriginScope shapes matching the OpenAPI spec exactly); tenant registry (`PracticeRegistryEntry` + `TenantRegistryDbContext` + EF Core migration + `ITenantRegistryService`) per ADR-001, schema inferred and flagged (§3 item 24); canonical `POST /auth/token` (MediatR `AuthenticateCommand`/handler, `EntraIdIdentityValidator` via MSAL client-credentials, `JwtTokenIssuer` minting HEK's own resource-scoped JWT per ADR-003), gated to return `501` since no source document defines its origin scope for a non-legacy caller (§3 item 26); three legacy compat auth endpoints built from the **actual documented legacy wire shapes** found in `docs/_source_docs/KARO_HSS_doc.md` and `ERMS_doc.md` (`HssAuthenticateTranslator` JSON, `ErmsCredentialTranslator` XML, both origin-scope-hardcoded per ADR-003's core "never trust a caller-supplied System field" correction) plus `ColCredentialTranslator` for the undocumented COL/Pegasus shape (explicitly flagged as inferred, since SRS confirms COL is undocumented); `ResourceScopeAuthorizationHandler` + `ResourceScoped`/`PlatformAdmin`/`BillingWrite` policies (built and unit-tested, not yet applied to any endpoint — that starts in Block 2); HISO session handling (`HisoSessionRepository` parameterized ADO.NET query against `[Appointment].[tblHealthLinkSession]`, `ResolveHisoSessionQueryHandler` adding the new 12-hour expiry + structured security-event logging replacing the old swallowed-exception pattern, `HisoSessionResolver` with ADR-007 fallback-search-on-miss); rate limiting (`Microsoft.AspNetCore.RateLimiting`, `RateLimitOptions.Enabled=false` by default per ADR-008); RFC 7807 error handling (`GlobalExceptionHandler` implementing .NET 8's `IExceptionHandler`, `ErrorCatalog` of generic non-PHI messages, `Domain/Exceptions` hierarchy mapped to the Contract Design doc §10 status table); CORS allow-list mechanism (`CorsOptions`, localhost-only dev placeholder). **Verified:** 8 unit tests passing (0 failures) covering `AuthenticateCommandHandler` success/failure paths, `ResolveHisoSessionQueryHandler`'s expiry logic (not-found/expired/success), and `ResourceScopeAuthorizationHandler`'s claim validation — consistent with the plan's own scoping of Block 1 verification to the handler level, since no Block 2 endpoints exist yet to test authorization against end-to-end.
- **New ADR-012** drafted (`docs/adr/ADR-012-solution-structure-and-block1-inferences.md`) documenting nine implementation-level decisions made during the build that weren't already settled by ADR-001–011: the added `Contracts` project, COL living in `Adapters.Erms/Col/` rather than a new project, one Dockerfile not one-per-service, the canonical-token 501 gating, legacy-compat status-code/path handling, and every flagged schema/config inference (tenant registry, HISO address map, HISO session-expiry column).
- **Genuinely unresolved, carried forward as new open items** (§3, items 23–27): the dormant-DAL-module SQL-injection-fix deliverable is **blocked, not built** — Zohaib confirmed defer-entirely given no legacy source exists in this repo (this leaves SRS Phase A hard-blocker item 4 open, tracked honestly rather than fabricated); tenant registry schema, HISO address map, and the HISO session-expiry column are inferred designs needing confirmation; canonical `/auth/token`'s origin scope for non-legacy callers is unresolved; legacy compat endpoints' path-namespacing and always-200 behavior is a reasoned default that should be confirmed against ADR-011's host-based routing model before being treated as final.
- **Known Day-1 issue, not yet root-caused:** `/health/ready` under a real run returns the correct *category* of response (non-200, confirming the liveness/readiness distinction works) but surfaced as a `500` via `GlobalExceptionHandler` rather than the health-check middleware's native `503 Unhealthy`, suggesting the `AspNetCore.HealthChecks.SqlServer` check is throwing rather than being caught internally as expected. Not chased further given Day-1 scope; worth a fix pass before Block 1 is considered hardening-ready.
- **Test coverage note:** `Domain.UnitTests`, `Infrastructure.UnitTests`, and `Adapters.UnitTests` currently have zero tests (Block 1's logic that needed testing lived in `Application` and `Api`) — a fuller unit/integration suite (including the mock-idp + `tblHealthLinkSession` fixture approach described in the approved plan) is deferred to the post-sprint hardening phase's dedicated test-suite stage, not silently treated as done.
- **Not yet done:** `docker compose up` end-to-end verification (Docker Desktop unavailable in this session); creating an actual GitHub remote (CI's "green on an empty commit" bar is therefore not yet directly observable — the workflow file itself is written and correct, but unexercised).

### 2026-07-19 (Phase 11 — Implementation Plan agreed, timeline reality-check)
- Zohaib asked to move straight from SRS/EAD/API Contract to Implementation Planning, skipping ahead of Phases 8-10 (Security Architecture detail, Logging & Observability Design, Infrastructure Design), all still Not Started. Flagged the gate per the Enterprise Technical Advisor skill ("never skip required phases") before proceeding. **Stakeholder decision: fold Phases 8-10 into the implementation plan as embedded build tasks rather than producing standalone docs** — not skipped outright, just not separately documented.
- Locked implementation inputs via direct Q&A: **.NET 8 LTS**, solo developer (linear, not parallelized sequencing), greenfield new repo/solution (legacy is .NET Framework 4.8/WCF/OWIN, not reusable for ASP.NET Core 8), GitHub Actions assumed for CI (stated as an assumption, not confirmed).
- **Timeline reality-check:** Zohaib initially asked for a 3-day full build, then "AI can do it in one day." Pushed back directly — code generation speed isn't the bottleneck for a PHI-handling system that needs verified auth, verified RLS isolation, a verified-fixed SQL injection, and load-testing at 10,000 concurrent users; none of that compresses to a day. **Resolved:** plan frames Day 1 as a full code-generation sprint only (complete draft codebase, not verified/production-ready), followed by a separately-paced, realistically-estimated hardening phase (tests, security verification, load testing, staged ADR-008 rollout) that is explicitly not compressed.
- Implementation plan agreed in chat, structure: **Day 1** = Block 0 (repo/Docker/logging/CI scaffolding, folding in Phases 9 & part of 10) → Block 1 (auth/tenant-routing/rate-limiting/SQL-injection-fix security core, folding in Phase 8 + Phase A hard blockers) → Block 2 (18 domain endpoint groups built in priority order, per the "implement everything, don't skip broken/dead endpoints" standing rule) → Block 3 (OpenAPI spec re-validated against actual build). **Post-sprint** = test suite (1-2 wks), security verification (1 wk), load testing (1 wk), staged rollout (ongoing, self-paced) — plus backfilling the two still-open items (KARO `SaveInvoice` field list, rate-limit thresholds).
- **Not yet done:** the plan has not been written to a document file — per Zohaib's working-style rule (no files without explicit go-ahead), it's confirmed inline in chat only so far.

### 2026-07-19 (§3 reconciliation)
- Zohaib asked "what's left, ask me" — before surfacing new questions, reviewed §3 against the ADR follow-up round (`Unified-Healthcare-API_ADRs.md` §7/§9) and found 10 of the 13 previously-listed open items had actually been resolved in that round but never removed from this table — a stale-log risk the skill exists to prevent.
- Moved all 10 resolved items into a new §2 "ADR follow-up round decisions" table; renumbered §3 down to the genuinely open items (cloud host, Azure/COL liveness, Aspose, security-toggle ownership, HISO/KARO dead-endpoint dispositions, two technical-verification items, rate limits, ERMS PHO-override parity).
- Proceeding to ask Zohaib directly about the four highest-priority genuinely-open items (security toggle ownership, Azure/COL liveness, HISO dead-code disposition, KARO mock-endpoint disposition) — see next entry for outcome once answered.

### 2026-07-19 (stakeholder Q&A on remaining open items)
- Asked Zohaib the four highest-priority genuinely-open items directly. Answers: (1) Zohaib owns the ADR-008 security-toggle switch-on decision, no date set yet; (2) the Azure ERMS mirror is not currently live but its capability must be kept in the platform, not deleted — COL/Pegasus's own liveness wasn't addressed and remains open; (3) HISO's dead code (`static mode`, `addInvoice`, `launchForm`, unreachable `saveProcessAction`) must be implemented, not skipped — reverses the Contract Design doc's Section 4.9 exclusion; (4) KARO's `GetEncounterSummary`/`SaveScreeningCode` must be implemented, not skipped — reverses Section 4.10/4.13's exclusion.
- Flagged one reading ambiguity rather than guessing silently: "implement as it is" for the two confirmed-broken KARO endpoints is being interpreted as "build the real capability, don't retire the endpoint" rather than "reproduce the broken/insecure legacy behavior," since the literal broken behavior (no auth check on `SaveScreeningCode`, hardcoded mock data on `GetEncounterSummary`) directly conflicts with FR-AUTH-04. Recorded as new open item #21 pending explicit confirmation.
- **Follow-up action identified, not yet done:** the API Contract Design document (`Unified-Healthcare-API_API-Contract-Design.md`) itself needs a revision pass to un-exclude the HISO dead-code actions and the two KARO endpoints from Sections 4.9/4.10/4.13, now that the "implement, don't skip" decision has been made. The document as currently saved still reflects the older "exclude, pending sign-off" position.
- Reconciled 10 stale §3 entries that had actually already been resolved in the ADR follow-up round but were never removed — moved to a new §2 table so future sessions don't re-ask settled questions.

### 2026-07-19 (SaveScreeningCode/GetEncounterSummary + rate limiting resolved)
- Zohaib adopted the recommendation given for the flagged conflict: implement `GetEncounterSummary`/`SaveScreeningCode` for real (working auth, working persistence/data) rather than reproducing their confirmed-broken legacy behavior, keeping existing names/paths/shapes for continuity; build rate limiting and apply the same config-toggle rollout pattern already used for auth/origin-scoping (ADR-008) rather than shipping with none. Closes open item #21 — **§3 open-decisions table is now fully empty** for the first time this project.
- ✅ **All four owed revisions applied (2026-07-19):** `Unified-Healthcare-API_API-Contract-Design.md` bumped to v1.1 — demographics split into 4 legacy-shaped endpoints (§4.2/6.2, no merge); HISO's `addInvoice`/`launchForm`/static-mode implemented, not excluded (§4.9); KARO's `GetEncounterSummary`/`SaveScreeningCode` implemented for real, not excluded (§4.10/4.13); rate limiting's config-toggle rollout detail added (§14); Section 8 gained Decisions 4–6 documenting each; Section 16 and 18 updated to match. Companion `Unified-Healthcare-API_openapi.yaml` bumped to v1.1.0, endpoint count 37→42, re-validated and cross-checked against the doc (exact match).

### 2026-07-19 (blanket "keep as old" answer — flagged conflict with hard blockers, not applied)
- Zohaib answered the three remaining open items at once: "keep it as it is implemented in old systems for all mentioned points," plus "if it is webconfig ignore it" for the ERMS PHO-override check.
- ERMS PHO-override item closed (config-only, not worth verifying) and KARO's `SaveInvoice` field list stance (keep as-is once found) both applied cleanly — no conflicts.
- **Not applied yet, flagged back to Zohaib instead:** taken literally, "keep as old" for `SaveScreeningCode`/`GetEncounterSummary` and for rate limiting would reintroduce two items this project's own SRS already classified as Phase A hard blockers — `SaveScreeningCode` shipping with no authentication check at all (FR-AUTH-04), and the platform shipping with zero rate limiting (SRS §12.9, Phase A item 7). Recorded in §3 item 21 as answered-but-unapplied pending Zohaib's explicit reconfirmation, rather than silently building known-insecure behavior into the new platform.

### 2026-07-19 (demographics: keep legacy endpoints separate, "as previously implemented")
- Zohaib decided not to merge HISO/KARO/ERMS's demographics (and by extension, other union-shaped) endpoints into one canonical shape — no sample responses or time available for the field-by-field comparison right now, deferred to a later check.
- Follow-up question asked (route-by-origin vs. fully separate endpoints) — Zohaib answered "as previously implemented," i.e. each legacy system keeps its own distinct endpoint/shape (no shared canonical path, no origin-based response switching). Confirms Option B.
- **Net effect on the contract document:** the merged `GET /patients/{patientId}/demographics` endpoint (Section 4.2/6.2 of `Unified-Healthcare-API_API-Contract-Design.md`) needs to be replaced with three separate, legacy-shaped endpoints. This is now the third pending revision to that document, alongside un-excluding HISO's dead-code actions and KARO's two broken endpoints (all decided in earlier sessions today, none yet applied to the file).

### 2026-07-19 (SQL-injection exception confirmed)
- Zohaib confirmed explicitly: "no injections don't stay as is." The "don't remove any old code" principle does **not** extend to the confirmed SQL injection in the dormant `DMSDA.cs`/`DBMessages.cs` DAL modules — those modules stay in the codebase (nothing deleted), but the injection itself must be fixed before being carried forward. Closes open item #22; no remaining conflict with the SRS's Phase A hard-blocker.

### 2026-07-19 (toggle self-management + code-preservation principle)
- Zohaib will flip the ADR-008 security toggle himself, with no fixed date being tracked — closed as self-managed rather than left as an open "date TBD" item.
- Zohaib stated a broadened instruction: **do not remove any code from the previous APIs, since it has impact.** Recorded in §2, but flagged rather than applied uncritically — this appears to conflict with an already-agreed hard-blocker security requirement (SRS Migration Phase A item 4: parameterize or remove the confirmed SQL injection in the dormant `DMSDA.cs`/`DBMessages.cs` DAL modules before carrying them forward). Working assumption recorded as new open item #22: keep the modules, fix the injection in place, rather than deleting the code — but this needs Zohaib's explicit confirmation since it's a security-critical interpretation, not a routine one.

### 2026-07-19 (cloud host decision)
- Zohaib resolved the last outstanding item from the prior round: host on **own servers for now**, but keep the Docker-based deployment (ADR-005) cloud-agnostic so a future move to AWS, Azure, or Google Cloud is a portability exercise, not a redesign. No cloud-vendor-specific managed service should be adopted without a self-hosted or portable fallback.
- §3 open-item count drops from 6 to 5 (plus the 1 self-owned by Zohaib, unchanged).

### 2026-07-19 (follow-up clarifications on the Q&A round)
- Zohaib clarified four items from the prior round: (1) "host isn't decided" explained in plain terms (which cloud provider runs the already-decided Docker containers — not yet chosen, not currently blocking); (2) COL/Pegasus confirmed **live** — its 7 endpoints stay in scope as active capability, not dormant; (3) Aspose confirmed **keep, implemented as the old system did it** — no replacement; (4) stated a general, platform-wide principle: **do not remove anything from the new API that any old API already implemented** — applies beyond KARO's `SaveInvoice` fields specifically, recorded as a standing constraint on every future contract/schema decision.
- ERMS's PHO-override parity with KARO (item #20) — Zohaib will verify this himself directly; recorded as self-owned, not a blocker for further work here.
- Net effect: open-item count in §3 dropped from 10 to 6 unresolved-here items (plus 1 self-owned by Zohaib, not counted as blocking). Remaining: cloud host, toggle enable-by date, demographics field reconciliation, KARO invoice field discovery, rate-limit thresholds, and confirming the "implement, don't reproduce broken behavior" reading for KARO's two confirmed-broken endpoints.

### 2026-07-19 (Phase 7 — API Contract Design)
- Ran the `api-contract-designer` skill against the SRS, EAD, and ADR log. Read all three legacy `EndpointInventory.md`/`BusinessRules.md` sets and the `ComparisonReport.md` duplicate-endpoint/duplicate-logic sections to build a canonical, traceable endpoint inventory (37 endpoints across 18 domain groupings).
- Resolved SRS §13.3's deferred response-format question by inference (not a fresh stakeholder question): one canonical JSON contract, with edge compatibility adapters preserving every existing consumer's exact wire shape — directly consistent with the "zero consumer-side change" pattern already locked into ADR-002/004/007. Documented as Reconciliation Decision 1.
- Asked the stakeholder three genuinely undecided contract questions (no ADR/EAD precedent existed for any of them): versioning strategy, error response format, and pagination. Stakeholder decisions: no version identifier in the contract (doc-only version tracking, zero functional disruption), errors must never display data (data-minimized RFC-7807-style shape, detail only in server logs), and no pagination in v1 (legacy third-party consumers have no pagination support on their end).
- Produced `Unified-Healthcare-API_API-Contract-Design.md` (21 sections) and a companion `Unified-Healthcare-API_openapi.yaml` (OpenAPI 3.0.3, 37 operations, YAML-validated and endpoint-count-cross-checked against the contract doc's Section 4).
- Self-scored Contract Review (§17): 4 PASS / 3 WARNING / 0 FAIL. The Security Exposure WARNING is explicitly called out as inherited from ADR-008's still-unresolved security-toggle-ownership item, not a new gap this document introduced.
- Six new open items added to §3 (dead-endpoint disposition, mock/broken-endpoint disposition, demographics field reconciliation, KARO invoice field list, rate-limit thresholds, security-toggle ownership carried forward) — all traced to specific sections of the new contract document rather than left as vague TODOs.
- Phase 7 marked Complete. Next phase is Phase 8, Security Architecture (detailed) — SRS §12 gave high-level direction via ADR-002/003/004, but a full threat model has not yet been built.

### 2026-07-19 (later same day)
- Zohaib uploaded the actual `Unified-Healthcare-API_EAD.md` (28 sections + Review Board validation, 8 PASS / 4 WARNING / 0 FAIL) — saved into this project folder at `docs/architecture/`.
- Zohaib confirmed no database schema redesign is needed — legacy Indici DB and stored procedures are connected to as-is, consistent with ADR-010.
- Flagged that this still leaves two genuinely database-architecture items open (new tenant-registry DB design, and the EAD's own flagged assumption that every RLS-protected table has a practice-identifying column). Presented as a choice; **Zohaib decided to skip Phase 6 (Database Architecture) entirely and verify the column assumption later only if it becomes a problem** — an explicitly accepted risk, not an oversight.
- Phase 6 marked Skipped. Next phase is now Phase 7, API Contract Design (needs to resolve SRS §13.3's deferred response-contract shape — KARO's JSON vs. ERMS's XML into one canonical format).
- Companion ADR document (`Unified-Healthcare-API_ADRs.md`) provided by Zohaib and moved into `docs/architecture/`, alongside the EAD. All 11 ADRs confirmed present. Action item closed.

### 2026-07-19
- Enterprise Architecture phase completed in a separate chat session: `enterprise-architecture-designer` skill was built, packaged, and run against the real SRS. Produced an ADR log (11 ADRs) and a full 28-section Enterprise Architecture Document, with an Architecture Review Board score of 8 PASS / 4 WARNING / 0 FAIL.
- Major correction surfaced during that session: the SRS's assumption of "one shared Indici PMS database" was wrong — real production topology is sharded across multiple physical DB servers per practice, confirmed directly by Zohaib. Architecture now routes via a tenant registry + RLS instead of assuming a single database (ADR-001).
- Reviewed transcripts of that session in this chat, updated this status log with the ADR summary, corrected production-topology facts, and consolidated open items (§3) from both this file's prior list and the ADR document's own "Open Items" section.
- Flagged as an action item: the ADR document and EAD file need to be copied into this connected project folder — they currently only exist in the other session's temporary output area, not here.

### 2026-07-18
- Connected `E:\claude_projects\hek_analysis` folder to this session; located and read the completed SRS (`docs/SRS_UnifiedHealthcareAPI.md`) and full analysis report set.
- Discussed and resolved the data isolation question for the unified platform: confirmed as module/operation-level authorization over shared clinical data (later formalized as ADR-003 on 2026-07-19).
- Created this status log at Zohaib's request, to be updated every session so future chats don't lose context.
